# Tasks

本目录是正式任务状态的事实来源。规格、验收标准与决策记录在 GitHub Issue；已完成任务与阶段的详细记录在 [`completed/`](./completed/)、Issue / PR 和 Git 历史。

## 当前状态

```text
阶段 1-8：Completed
当前阶段：本项目源码阅读（工作台第 0 档）
Active Agent Task：#170（已实现、待验收）
Gated：#117 Responses API adapter（2026-09-18）、web_fetch（2026-09-19），触发条件见看板
产品方向：内部数据工作台（2026-09-20 定案，docs/research/workbench-direction.md），未立 Issue；档、顺序与触发只在其第 7 节
候选子系统：session 事件流与 replay、审批门、compaction、定时任务（未立 Issue，各自的档见 workbench 第 7 节）
学习环节待做（带读收尾与独立改一处，均无「学习已验证」评论）：#115、#116、#126、#127（Issue 只豁免讨论阶段复述，预测题未出不算豁免）、#134、#135、#136、#137、#142（Issue 豁免预测题）、#144、#146、#151、#152、#153、#154、#155、#156、#157、#158
Admin Task 4：Planned
```

## 看板

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| #167 落库文本清洗（U+0000 不再让 Run 停在 RUNNING） | Completed | 实施状态：已实现 / 验收状态：已通过。PR #171 代码 head `da358c8` 的 AC-01～AC-07（含 AC-05b）于 2026-09-23 逐条 PASS（PR 验收评论为证据）：可见 delta、Grounding 回答、用户消息在进入 `content` 前把 U+0000 与孤立代理项换成 U+FFFD（delta / done / 落库同一个串），工具 Step 的 callId / toolName / errorMessage 与 debug 抓取信封同样过 `toPersistableText`（移到 `agent-runtime/persistable-text.ts`）；实现会话读码补入 debug 抓取入口、review 后孤立代理项也在 `content` 入口替换（Issue 已更新）。验证：api typecheck / lint、`test:model-stream` 105、`test:grounding` 159、`test:agent-recorder` 22、`test:grounding-db` 22（新增 7 条真实 DB 故障注入，旧代码上 7 条全失败且复现「终态收口失败，DB 状态可能停留在非终态」；只还原抓取信封时 AC-05b 以 jsonb 报错失败）、`test:tools` 80、`test:chat-service` 29。`/code-review high` 两轮 15 条修 10 条。限制：跨两个 delta 的代理对会各自换成 U+FFFD（上游按码点解码，实际不出现）；`Conversation.title` 含 U+0000 仍返回 500（Issue 不做其他表）；不清理已卡住的 Run（R2）。学习环节待做 |
| #168 模型调用边界补漏 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #172 代码 head `6f6be0c` 的 AC-01～AC-07 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：未映射状态码文案里本次 key（≥ 8 字符）先换 `***` 再截断（Codex P1，PR #159），SDK `logLevel: 'off'`，上游 JSON 解析失败与既无 `choices` 也无 `usage` 的数据块归 `LLMApiError`（只带 usage 的末尾块、省掉 delta 的 choice 放行），流内 error 对象的 4xx / 5xx code 与响应状态码同表归类，`firstTokenMs` 只认生成事件，缺 index 与显式 index 经 `ToolCallSlots` 分槽（adapter 与 tee 共用，谁先到都不撞号）。验证：`pnpm typecheck`、api / ai lint、`@agent/ai` 76、`test:model-stream` 111、`test:llm-config` 23、`test:grounding` 159、`test:chat-service` 29；新增 15 条 AC 用例在旧代码上全失败。`/code-review high` 两轮 17 条修 11 条。限制：`firstTokenMs` 口径变了，概览 P50 一段时间内新旧混算、空正文轮次不再进样本；缺 index 与显式 index 以 1 / 2 / 0 这类交错顺序出现时调用顺序可能变；流内错误的 warn 日志无 `httpStatus`（HTTP 本身是 200，分类在 errorCode）；截断后的文案仍可能带中转站自己的上游 key。学习环节待做 |
| #169 前台 Markdown 与会话小修 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #173 代码 head `ecfc5f4` 的 AC-01～AC-09 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：接入 `markdown-it-cjk-friendly`（新增运行时依赖，`@types/markdown-it` 升到 14.2.0）；流式补齐不补两侧 ASCII 字母数字、或单个 `*` / 紧贴在词里的标记后接 `.` `,` `;` `:` `=` `/` `)` `]` `}` `?` `!` 的 opener，尾块最后一行已换行时按终态显示；解析器认定的表格按单元格切分，前一个字符是 `\` 的竖线不是边界（与 markdown-it 一致）；尾块末尾只有 ATX 标题标记（含列表项里的）时先不显示；流结束后到期前一帧不等提交间隔、一次放完（150ms 内）；linkify 与 `BARE_URL` 在全角标点处截断；图片在链接内的判断每组 inline token 只遍历一次；runtime 在用户消息落库时记 `userMessagePersisted`，经 run_failed 与 error 事件下发（不下发 runId），前台据此更新侧栏。验证：`pnpm typecheck`、web / api lint、web `test` 91（新增 10）、`build`、`test:e2e` 30（新增 2）、`test:chat-service` 29；新增单测在旧实现上 10 条全失败，AC-08 正向 e2e 在旧 hook 上失败。`/code-review high` 两轮 14 条修 9 条。限制：流式中间态按「宁可少补」，`v**2` 这类英文词内强调与以全角标点开头的强调在闭合前显示字面标记；路径里本就带全角标点的 URL 会被截短；start 之前用户停止时侧栏不同步（刷新对齐）；AC-07 是墙钟性能断言。学习环节待做 |
| #170 模型配置校验小修 | Active | 实施状态：已实现 / 验收状态：待验收。`updateModel` 只要合并后的行可见就按 `resolveInitialContextBudget` 校验（隐藏行不校验，坏行可先藏起来）；服务商弹窗 watch 家族 / 地址 / 密钥，变化时经 `setFormCredentials` 清掉旧名单与测试结论（末尾斜杠不算变化），提交时凭据与测出结论时的表单凭据不一致也不带结论；`resolveLlmEnvConfig` 拒绝 #156 前的公开占位主密钥并单独提示。验证：`pnpm typecheck`、api / admin lint、`test:llm-config` 27（新增 4）、admin `test`（新增 1 组 check）、admin `build` 与 `test:e2e` 13；新用例在旧实现上全失败 |
| #151 模型调用失败保留真实原因（errorCode / 首 token 时间） | Completed | 实施状态：已实现 / 验收状态：已通过。PR #159 代码 head `1706a2d` 的 AC-01～AC-07 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：contracts 加 `AGENT_RUN_ERROR_CODES`，`AgentRun.errorCode` 可空列（migration 只加列）；runtime 在终态确立后由 `describeRunFailure` 一处得出 errorCode 与文案，Run、失败采样 / finalization / 工具 Step 与 error 事件同源，errorCode 与终态同一条 Run CAS 写入，流错误保留 `cause`，FAILED 时一条 `agent_run_failed` warn；`getAiExceptionMessage` 移到 `common/utils`；`@agent/ai` 加 `reasoning_started`、流正文阶段 abort 不再报成缺 finish reason、raw capture 按 finish_reason / usage / signal 标 partial、上游 5xx 统一 `LLMServerError`；采样 Step 记 `firstTokenMs`（含 SDK 首个响应头前的重试与退避）；Admin Run Trace 头部与采样详情展示两项。验证：真实 SDK + fake fetch 故障注入覆盖 401 / 402 / 429 / 500 / 连接重置 / 缺 finish_reason / 用户停止 / deadline / finalization 401；`pnpm typecheck`、api / ai / admin lint、`test:model-stream` 94、`test:agent-recorder` 21、`test:grounding` 156、`@agent/ai` 55、`test:admin` 66、prisma generate / validate；dev 库 `migrate deploy` 后 27 条旧 Run 为 null、Admin 显示「未记录」；本机临时错误 key 服务商 → 前台「AI 服务认证失败…」与 Run Trace `llm_auth`（测后删除），正常对话首 Token 时间 503ms。`/code-review high` 两轮 18 条修 11 条。限制：408 / 409 等未单独归类的 4xx 仍归 `llm_protocol`；dev 库既有 `LlmProvider.family` 默认值漂移与本改动无关；消费者 `return()` 兜底收口的采样 Step 没有 usage 与 `firstTokenMs`（2026-09-23 复核补记）。学习环节待做 |
| #146 中转站各家族协议差异收口 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #147 代码 head `0b5894c` 的 AC-01～AC-06 于 2026-09-22 逐条 PASS（PR 验收评论为证据，含 fixture 字段名改动的故障注入）：`LLM_FAMILY_CAPABILITIES` 扩成 compat 表（`thinkingFormat` / `requiresReasoningContent` / `reasoningEfforts`），`LLMModelProfile` / `ResolvedChatRequestConfig` 用 `compat` 替换 `reasoning: boolean`，删 `isThinkingFamily`；usage 缓存字段按 `prompt_tokens_details.cached_tokens ?? prompt_cache_hit_tokens ?? cached_tokens` 兜底、只有命中数时推导未命中数；新增五个家族（deepseek 直连 / 经中转、grok、gpt、gemini）的真实响应 fixture 与手工 chunk 序列及只读导出脚本；web e2e 模型桩对齐 `ChatModelOption[]`。验证：`@agent/ai` 51 项单测、api `test:llm-config` / `test:model-stream`、全仓 typecheck、ai / api lint、web e2e 21 项（含 `reasoning-effort.spec.ts`）通过；本机 grok 与经中转 DeepSeek 各两轮，第二轮 `promptCacheHitTokens` 分别为 2304 / 1280，与原始 `cached_tokens` 一致；经中转 DeepSeek 带工具调用一轮在 `requiresReasoningContent: true` 下正常完成。限制：`*.response.json` 不含 `reasoning_content`（落库时已剥离），`packages/ai/scripts/` 只过 lint。学习环节待做 |
| #149 删 #119 后无读者的初始上下文观测字段与 Admin Context Inspector 来源分区 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #150 head `a7fbe33` 的 AC-01～AC-05 于 2026-09-22 逐条 PASS 并合并（merge `55c56f2`）：contracts / 投影 / runtime 落库 / admin UI 四层删 `historyCandidateCount / historyIncludedCount / samplingHistoryExcludedCount / observations / providerItemCount` 及 Step #1 的 `candidateCount / excludedCount / excludedReason`，「Context 结果」保留；planner 内部 `summary` 不动。验证：全仓 typecheck、`test:context` / `test:model-stream` / `test:admin`、admin 三个 check、api / admin / contracts lint。源码阅读中的顺手改动，无学习环节 |
| #144 前台模型对话 UI 优化 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #145 代码 head `c8f903d` 的 AC-01～AC-06 于 2026-09-21 逐条 PASS（PR 验收评论为证据）：限高代码卡片、80ms Markdown 批处理、阅读定位 / Loading 胶囊、Tooltip；修复 Markdown 结构与代码空白、终态卡片重建、暗色样式、返回生成会话后的跟随、会话恢复竞态、复制失败反馈与锁文件残留；删流式前缀缓存及无消费者 Tooltip 配置。web typecheck / lint / build、54 项单测、10 项 Chromium 回归通过；独立 reviewer 的 2 条 finding 均浏览器先复现失败后修复并定向复核。限制：未做 CPU 基准 / 真实 Provider / 人工视觉验收；顶层围栏使用卡片，嵌套结构保留标准 Markdown，超过20,000字符纯文本，预览不可用。80ms 批处理已被取代：`c9c49ef` 改为按帧平滑放出，#155 再把 delta 按帧合并（2026-09-23 复核补记）。学习环节待做 |
| #142 模型配置入库 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #143 于 2026-09-20 基于最新 head `87cefcc` 逐条验收 AC-01～AC-11 PASS 并合并（`39edad1`，Closes #142）：新增 `LlmProvider` / `LlmModel` 与三条 migration（建表、删 `reasoning` 列、加 `reasoningEffort` 列），Provider API Key 用 `AGENT_SECRET_KEY` 派生密钥 AES-256-GCM 加密入库、只回显尾四位；删 `LLM_API_KEY / LLM_BASE_URL / LLM_MODEL` 与 `packages/ai` 硬编码模型表，client 按 Run 快照里的凭据构造；思考协议改按家族固定（`@agent/contracts` 的 `LLM_FAMILY_CAPABILITIES` 一张表：deepseek 走 thinking + `reasoning_content` 校验，其余不走），模型行 `reasoningEffort` 按家族取值、请求级可覆盖，直打中转站实验证实 GPT / Grok 透传该参数（Gemini 暂不开放）；导入预设按家族官方上限（DeepSeek 1M/384k、GPT-5.6 1.05M/128k、Grok 4.6 500k/128k、Gemini 3.8 Flash 1M/64k）；`admin-llm` 模块：Provider / Model CRUD、`providers/fetch-models` / `test-models`（有 apiKey 用表单的，否则按 providerId 用库里的）、导入随行写入弹窗测试状态、probe 按真实 Run 参数；管理台「模型接入」页：默认改图钉、状态图标即重测、推理强度列可直接选；前台按家族显示思考强度选择器。本机冒烟：DeepSeek 与 Gemini 各一次带工具调用的对话到 done，Run Trace 采样 Step `providerId / modelId` 与配置一致；不可见 / 不存在 / 停用 / 无默认均 400 中文提示；缺 `AGENT_SECRET_KEY` 启动失败并提示。验证：`pnpm typecheck` 6/6，api / web / admin / ai / contracts lint，`@agent/ai` 41、`test:llm-config` 7、`test:chat-service` 29、`test:tool-loop` 5、`test:model-stream` 82、`test:tools` 80、`test:retrieval` 34，`prisma validate`。`/code-review high` 9 条全部修复；可维护性审查 15 条改 8 条（净删约 430 行）。学习环节（带读 + 独立改一处）待做 |
| #118 删除死代码、单实现抽象与自校验 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #121 于 2026-09-17 基于最新 head 逐条验收 AC-01～AC-06 PASS 并合并（+327 / −761，无新增文件）：删 `receive_user_message` Step 写入、`abortStep`、`ToolRegistryService.require / listDefinitions`、`ContextBudgetExceededError.stage`、`SeoContextBuilder`、`AgentRunConfigurationService`、`ToolExecutionContext.executionAttempt`、`ModelContext.forSampling` 与 snapshot 明细项，`TokenEstimator` 改 interface；`tool-step-summary`、Admin 读取字段与 `tool-evidence` 校验按 Issue 保留。学习环节按 Issue 决策记录豁免 |
| #119 历史裁剪合一 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #122 于 2026-09-17 基于最新 head `bcf76b0` 逐条验收 AC-01～AC-06 PASS 并合并：删 `InitialContextSelectionService` 分页与批内二分，历史一次 `findMany({ take: hardLimit })`，首轮由 planner `excludeOldestHistory` 裁剪；`initialContext` 取裁剪前值（`excludedReason` 只剩 `candidate_cap`）、`contextPlan` 记删减数；`buildModelMessages` 改 `instructions`；删 `SEO_CHAT_HISTORY_CANDIDATE_BATCH_SIZE`。真实 tokenizer 差分旧算法 68 = planner 68；本机运行服务两轮真实对话 + Admin 投影冒烟通过。学习环节按 Issue 决策记录豁免 |
| #120 抽出 `packages/ai` | Completed | 实施状态：已实现 / 验收状态：已通过。PR #123 于 2026-09-17 基于最新 head `5840d98` 逐条验收 AC-01～AC-05 PASS 并合并：新建 `@agent/ai`（零 Nest、零 Prisma），`git mv` 迁入 `OpenAICompatibleClient`、流适配、tool-call 累加、raw capture、`llm.types` / `llm.errors` / `model-*.types` / `model-profiles`、`resolveLLMRuntimeConfig` 与 6 个测试文件，`JsonSchemaProperty` / `JsonObjectSchema` 同迁；client 去装饰器改收 `LLMRuntimeConfig` 纯对象，`LlmModule` 用 `useFactory` 装配；`apps/api/src/llm/` 只剩四个壳；七个 build 脚本加 ai build，`test:llm-config` / `test:model-stream` 按归属拆分，包内 `test` 先建 contracts；`index.ts` 只导出 api 实际引用。验证：删三处 dist 后 typecheck 6/6、api build 通过，`@agent/ai test` 37/37（无 `DATABASE_URL`），api 13 个不依赖数据库的 `test:*` 全绿；根 lint 仅 11 个 `docs/research` 文件为既有基线失败（与 master 逐文件一致）；AC-04 走本机 3002 dev 服务两次真实对话（start → delta → done、delta 拼接等于最终内容、Admin Run COMPLETED），`smoke:grounded-answer` 因隔离库 public schema 未跑 migration（P2021）无法执行。`/code-review high` 两轮 11 条：修 7 条、按 Issue 范围不修 4 条。学习环节按 Issue 决策记录豁免 |
| #124 `packages/ai` 目录按 Pi 分层整理 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #125 于 2026-09-17 基于最新 head `5e6134f` 逐条验收 AC-01～AC-06 PASS 并合并：根目录收成 `types.ts` / `errors.ts` / `config.ts` / `deepseek.ts`，`clients/` 改 `api/` 并按 `openai-completions*` 命名，12 个 `git mv`（11 个 R094～R100；`deepseek.ts` 因按范围第 3 条并入 DeepSeek REST 类型为 R073，AC-02 的 ≥ 90% 对该文件与范围第 3 条算术上不相容，PR 已说明），四个 `*.types.ts` 合并为 `types.ts`，`index.ts` 导出集合 34 个符号不变，零运行时行为变化；另修正 5 处研究文档旧路径。验证：删 dist 后 `@agent/ai` typecheck / build 通过、test 37/37，全 workspace typecheck 6/6，api typecheck / lint 通过。`/code-review medium` 1 条 Low（文档行号锚点）已修。学习环节按 Issue 决策记录豁免 |
| #126 Admin Run 读模型去过度设计 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #128 于 2026-09-18 基于最新 head `a7a8cf6` 逐条验收 AC-01～AC-08 PASS 并合并（47 files，+1,386 / −6,997，净减 5,611）：契约收缩为运维真正要的事实（删 9 个类型、常量 / 重复 / 可信度字段与 `safeRawData`，列表项 token 字段合并为 `usage`），四个 projector 合计 850 行、逐字段「能读就读、读不出就 null」、不做跨字段与跨 Step 校验，runtime 停写被删字段（零行为变化），Admin 只删展示项并用 `stepId` 关联 timeline；旧库 Run 全部 known Step、`receive_user_message` 投影为 generic；本机 dev 服务真实对话 + 查库核对 + Playwright 打开 Run Trace 零 console 错误。验证：typecheck 6/6、api / admin lint、api 7 组测试与 admin 三个 check 全绿。`/code-review high` 两轮 11 条：修 10 条，`grounding: true` 整行读取按 AC-01 保留。Review 遗留补修 PR #130 于 2026-09-18 基于 head `73c9652` 验收 PASS 并合并（`3a488bc`）：`chunkId` 身份原样保留、`evidenceRefCount` 未知时为 null；F-03 / S1 记为待决策不改，F-04 按 Issue 不采纳。学习环节：待带读与独立改一处，通过后在 Issue 追加「学习已验证」 |
| #127 收敛 `packages/ai` 运行时配置 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #129 于 2026-09-18 基于最新 head `95e74c6` 逐条验收 AC-01～AC-06 PASS 并合并（7 files，+75 / −216）：`LLMRuntimeConfig` 收为 `{ apiKey, baseUrl, model, captureModelIO }`，三个 timeout 改 `openai-completions.ts` 常量、`max_tokens` 默认 65_536 改 `config.ts` 常量，删应用硬上限层、`readPositiveInteger` 与 4 个 `LLM_*` env；`config.test.ts` 11 → 9 case，`llm.module.test.ts` 改断言 `captureModelIO` 默认 false 与 `LLM_MODEL` 不支持时初始化失败。验证：`/ai` typecheck / test 35/35 / build，api typecheck / lint，`test:llm-config` 2/2、`test:model-stream` 78/78，`git diff --check` 通过；AC-01 grep 零匹配。`/code-review high` 两轮 7 条：修 5 条（含第二轮收回超范围的默认 `max_tokens` clamp），残留 env 静默忽略与 `chat` 60s 数值按 Issue 范围不改。学习环节：Issue 只豁免讨论阶段复述，带读收尾与独立改一处待做 |
| #115 模型调用重试与 Loop 默认上限 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #131 于 2026-09-18 基于最新 head `4efc5b2` 逐条验收 AC-01～AC-06 PASS 并合并（7 files，+329 / −29；AC-01 字面 grep 的 5 处 `readPositiveInteger` 是 #126 Admin 投影 safe reader，origin/master 已存在、与本 Issue 无关）：`createClient()` 交给 SDK 内置重试的 `maxRetries` 改为代码常量 2（首个响应头之前；流正文中断、abort 后不重试），无新增 env；review 后补两处：`chatStream` 内 `rejectOnAbort`（SDK 退避 sleep 不监听 signal 且 `retry-after` 无上限，abort 落在 sleep 期间也立即抛出）与 `AbortSignal.any` 派生一次性信号（SDK 每次尝试在 signal 上挂监听不移除，10 轮 + 1 次重试即触发 `MaxListenersExceededWarning`）；policy 默认 `maxSamplingRounds` 3 → 10、`maxToolCalls` 2 → 8，`.env.example` / README 同步；`openai-completions.test.ts` 新增 8 个用例（1 个 `maxRetries` 常量断言 + 7 个 fake fetch：首次 429 / 503 / 连接错误后成功且 `onRequest` 只记一次、400 / 401 / 402 不重试、重试耗尽抛对应 `LLMError`、流正文中断不重试、abort 不重试、abort 落在退避 sleep 期间立即抛出、12 轮共用 signal 不累积监听）。验证：`@agent/ai` typecheck / test 43/43，api typecheck / lint、`test:tool-loop` 65/65、`test:tools` 86/86、`test:llm-config` 2/2、`test:model-stream` 78/78、`test:admin-runs` 55/55、`test:grounding` 172/172。Review：`/code-review high`（Opus）+ Fable agent 各 3 条、Fable 复审 2 条，修 5 条（abort 竞速、监听隔离、上限耦合提示、看板文案、timeout 注释），非流式 / metadata 请求无 signal 的最坏耗时按范围记录不改。学习环节：待带读与独立改一处，通过后在 Issue 追加「学习已验证」 |
| #116 同轮文本 + 多 Tool Call | Completed | 实施状态：已实现 / 验收状态：已通过。PR #132 于 2026-09-19 基于最新 head `2d62cfd` 逐条验收 AC-01～AC-10 PASS 并合并（34 files，+1,276 / −763）：sampling decision 只按 finishReason 分派，`SamplingDecision.tool_call.calls[]` 与 `assistant_tool_call.calls[]` 承载同轮多 call，顺序执行、每 call 一个 `tool_execution` Step、预算按 call 计数并在执行前整批拒绝；`length` 截断整批不执行、记 `truncated_arguments` 回喂；adapter 第四条不变量加 `length` 例外、`finalize()` 判重复 id；policy 删 `maxToolCalls < maxSamplingRounds`；删 `ChatMessage` / `toModelInputItems`，finalizer 删 `extra_event_after_completion` / `missing_submission`；DeepSeek estimator 裸 NaN / Infinity 守卫忽略字符串字面量。AC-03 按范围第 4 条「语义不变」：超预算批次不建 tool Step，原因记在 Run 的 errorMessage。验证（验收基于 head `2d62cfd` 复跑）：workspace typecheck 6/6，api / ai / contracts / admin lint，`@agent/ai` test 47/47，`test:tool-loop` 68/68、`test:model-stream` 81/81、`test:grounding` 174/174、`test:context` 29/29、`test:tools` 86/86、`test:seo-service` 32/32、`test:llm-config` 2/2、`test:admin-runs` 55/55，`grep ChatMessage apps/api/src` 为 0，`git diff --check` 通过。`/code-review high` 两轮 12 条：修 8 条（finalizer 去非空断言、adapter reasoning 不变量覆盖 length、observation 加 `resultIndex`、删 fixture 死字段、contract 删死值加反向守卫、estimator 裸 NaN / Infinity 守卫忽略字符串字面量、截断路径显式 `!argumentsTruncated`、`assistant_output` 顺序注释）；Session 同轮建立前已推出的文本、截断 raw arguments 原样回填、contract 删两个死值按 Issue 决策与 #126 原则不改。剩余风险：截断 raw arguments 原样回填未做真实 Provider 冒烟（官方 `encoding_dsv4.py` 有非法 JSON 回退，判断服务端容忍）。验收补修 PR #133（Refs #116）于 2026-09-19 基于 head `cdcb0ab` 逐条验收 PASS 并合并（6 files，+300 / −10）：实施状态：已实现 / 验收状态：已通过。PR #132 Codex 行内 P2 与外部验收审查同时指出：未经工具输入契约校验的原始参数（`unknown_tool` / `invalid_arguments` / `truncated_arguments`，前两条 #116 前已存在）回填后，生产 `DeepSeekV4TokenEstimator` 对 `[]` / `null` / `1` / `{"0":"x"}` / `{"nested":[1.5]}` / `{"x":1e999}` 等形态抛 `ContextTokenEstimationError`，下一轮 Run FAILED，违反 AC-06「Run 继续」；修复：`ModelContext.appendToolExchange` 按 `argumentsValidated` 决定续轮表示，未校验参数统一以官方回退形状 `{"arguments": raw}` 承载（原文保留在值里，wire 为合法 JSON，estimator 与实际请求同一份表示），已校验参数原样、固定向量不变、estimator 守卫不删；新增经真实 adapter → Runtime → Planner → 生产 estimator 的 13 形态回归与 wire body 断言。验证（基于 head `cdcb0ab` 复跑）：typecheck 6/6，`@agent/ai` 48/48，`test:tool-loop` 70、`test:model-stream` 83、`test:context` 29、`test:grounding` 174、`test:tools` 86、`test:admin-runs` 55 全绿，api / packages lint 与 `git diff --check` 通过；真实 Provider 冒烟未执行。`/code-review high` 1 条 Low（policy 拒绝的 `execution_failed` 边界，当前不可达）按范围不改，与 AC-03 口径差异（Issue 写「Step 记录原因」，实现为原因记 Run.errorMessage、不建 tool Step）一并记入 Issue #116 决策记录评论 |
| #117 Responses API adapter | Gated | 2026-09-18 关闭（not planned）：DeepSeek 上 Responses 与 Chat 无能力差异且无状态，为证明边界写第二实现属过度设计；Pi 的 DeepSeek 也走 completions。映射规格保留在 Issue，满足任一条件 reopen：DeepSeek 弃用 Chat Completions / 需要仅 Responses 有的能力 / 接入只支持 Responses 的第二 provider |
| #134 去 SEO 产品命名 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #138 于 2026-09-19 基于最新 head `c249f7c` 逐条验收 AC-01～AC-06 PASS 并合并（67 files，+759 / −944）：后端 `seo/` → `chat/`（`ChatModule` / `ChatController` `@Controller('chat')` 路由 `/api/chat/stream` / `ChatService` / `ChatDto` / `agent.prompt.ts`），删非流式 `chat()` 链路与 `SeoChatResponse`，`buildSeoAgentInstructions([])` 改为常量 `AGENT_INSTRUCTIONS`，手写 `StreamResponse` 改用 `node:http` `ServerResponse`（`@types/express` 未安装，不加依赖）；系统提示词改通用助手、保留贾维斯、工具段改「站内文章知识库」口径；env `SEO_CHAT_HISTORY_CANDIDATE_HARD_LIMIT` → `AGENT_HISTORY_CANDIDATE_HARD_LIMIT`，`.env.example` / `docker-compose.yml` 示例库名 `agent_ai_seo` → `agent`（既有卷与真实库不动）；contracts `seo.ts` → `chat.ts`（`CHAT_MESSAGE_MAX_CHARS` / `ChatRequest`，删 `ChatStreamProtocol` / `ChatStreamEventType` / `SeoChatResponse`）；前端 `components/seo/` 拆为 `components/home/` + `components/chat/`，`useSeoWorkspace` → `useChatWorkspace`、`api/seo.ts` → `api/chat.ts`、`SeoWorkspaceView` → `ChatWorkspaceView`，`Seo*` 类型 / hook / 变量去前缀；i18n 与流程图 / 简报节点文案改为知识库问答口径；CSS class 与 `<style>` 按 AC-02 零变化保留 `seo-*` 类名。验证：typecheck 5/5、api / web lint、`test:chat-service` 28、`test:tool-loop` 70、`test:model-stream` 83、`test:grounding` 174、`test:context` 29、`test:tools` 86、`test:retrieval` 42、`test:llm-config` 2、`test:admin-runs` 55、web test 44、`@agent/ai` 48、web build 通过；本机 dev 真实对话 `/api/chat/stream` start → delta×41 → done，Admin Run Trace 正常。AC-01 剩余 372 处命中逐类列在 PR：`.vue` 148 处为 class / `<style>` / SVG defs id（AC-02 保留）、6 处 `seo_title` / `seo_description` SQL 别名、约 200 处测试 fixture 数据、5 处本 Issue 回归守卫、smoke / baseline 9 处归 #135；AC-04 `grep -w` 为 0。`/code-review high` 1 条 Low（旧卷 + 新 `.env.example` 库名不一致）已按 README 说明修复。与 Issue 措辞差异：`StreamResponse` 改用 `node:http` `ServerResponse` 而非 Express `Response`（`@types/express` 未安装，不加依赖）。学习环节：待带读与独立改一处，通过后在 Issue 追加「学习已验证」 |
| #135 删离线评估与 smoke 死链 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #139 于 2026-09-19 基于最新 head `eb767ea` 逐条验收 AC-01～AC-06 PASS 并合并（19 files，+73 / −1,811，净减 1,738 行）：删 v1 baseline 闭环（`retrieval-baseline.ts` / `.cli.ts`、`retrieval-evaluation.ts` / `.test.ts`）与 `eval:retrieval-baseline`，`validateRetrievalResult` 原样搬入 `article-retrieval-quality-v2.ts` 作内部函数，`ArticleRetriever` 去泛型默认值；删 `grounded-answer.smoke.cli.ts` / `embedding-smoke.cli.ts` 及各自测试与 `smoke:grounded-answer` / `smoke:embedding`，`test:grounding` / `test:article-indexing` / `test:retrieval` 去掉已删测试项；`retrieve-article-context.smoke.cli.ts` 保留，去 `RetrieveArticleContextSmokeSummary` 接口与三个 `export`，默认查询词「SEO 标题应该怎么写」→「Wuthering Waves 的 soft pity 是什么」（#134 遗留）；删空目录 `insight/`、`dev:watch` / `predev:watch` / 根 `dev:api:watch`，三条 `test:admin-*` 合成 `test:admin`（一次 build 跑 5 个 dist 测试），`agent-runtime.service.test.ts` 只归 `test:model-stream`；`.env.example` 删 `GROUNDED_ANSWER_SMOKE_*` 段、`RETRIEVAL_TOOL_SMOKE_QUERY` 同步默认值；docs 旧路径 5 处加「已迁移至 / 已删除」标注，phase-08 归档补 baseline 已删标注。验证：typecheck 6/6、api lint，`test:retrieval` 34、`test:article-indexing` 60、`test:grounding` 154、`test:tools` 86、`test:admin` 63、`test:model-stream` 83、`test:tool-loop` 5 全绿；无 DB 环境 `eval:retrieval-quality` / `smoke:retrieval-tool` 进入 main 输出安全失败 JSON，两个 CLI 模块直接 import 可解析；`git diff --check` 通过。AC-01 剩余命中为 docs 历史叙述：`phase-08-grounded-retrieval.md:46-47`（46 行已加「已由 #135 删除」标注）、#120 记录的 `smoke:grounded-answer` 无法执行；AC-03 生产代码改动为 `article-retrieval.ts`、`article-retrieval-quality-v2.ts` 与删除的 `retrieval-evaluation.ts` / `retrieval-baseline.ts`（范围 1）。`/code-review high` 无 finding。学习环节：待带读与独立改一处，通过后在 Issue 追加「学习已验证」 |
| #136 零行为变化小修 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #140 于 2026-09-19 基于最新 head `03a5713` 逐条验收 AC-01～AC-07 PASS 并合并（49 files，+663 / −1,486）：tools 删 `risk / requiresApproval / idempotent` 与 17 行准入门、`model-tool-spec.mapper.ts` / `tool.errors.ts` / `TOOL_NAME_PATTERN`、`ValidatedToolInvocation.callId / toolVersion / samplingAttemptId`、`ToolExecutionContext.runId / conversationId`、`UnvalidatedToolCallEnvelope.samplingAttemptId`（runtime 改用循环内同值变量）、`ToolResult.data` 与第二泛型，三份工具清单合成 `tools/tool-definitions.ts` 的 `TOOL_DEFINITIONS`；agent-runtime `InitialContextSummary` 14 → 6 字段、快照只估算必带内容一次，service 直接 import recorder 的 `CloseAgentStepInput / CloseAgentStepMetadata`，`fromFinishReason` 改查表，四个事件 interface 内联，内联 `toLlmRole / isAbortSignalTriggered`，module 只 export `AgentRuntimeService`；admin / contracts 七个 allowlist 改 contracts `as const` 数组派生 union、删 `AssertAssignable` 守卫、导出 `AdminRunKnownTimelineItemBase`、五个镜像接口改 `Prisma.AgentRunGetPayload`、`context-inspector.projector.ts` 并回、测试夹具抽 `projection/__fixtures__.ts`、删 `readPositiveInteger` 与 contracts 四个零引用导出、`admin-overview` 复用 `readString(o, k, Infinity)`、`createAppValidationPipe()` 无参。`/code-review high` 2 条 Low（`parseProviderBalance` 空串需保持 null、configuration-map 仍列已删字段）已修，另按建议保留模型可见 schema 边界断言。验证：typecheck 6/6，api / admin / contracts lint，`test:tools` 80、`test:tool-loop` 5、`test:model-stream` 83、`test:context` 29、`test:grounding` 154、`test:chat-service` 28、`test:llm-config` 2、`test:agent-recorder` 21、`test:retrieval` 34、`test:admin` 63，admin 三个 check 全绿；本机 dev 服务真实对话 Run `cmu8b339y0002wigwh79o8bs6`（16 Step、grounded finalization `unavailable / insufficient_evidence`、COMPLETED）+ Admin Run Trace / Retrieval / Context Inspector 零 console 错误（`retrieve_article_context` 30s timeout 为 dev API 进程无代理环境所致，与本 Issue 无关）。与 Issue 措辞差异：AC-01 的 `samplingAttemptId` 按 `apps/api/src/tools` 范围解读；「四个 reader 合一」评估后不做（需新增谓词抽象）；DTO 测试用 `createAppValidationPipe()` 而非裸 `new ValidationPipe`；`ToolsModule` 三次 register 保留显式 definition / executor 配对。学习环节：待带读与独立改一处，通过后在 Issue 追加「学习已验证」 |
| #137 embedding 重试收敛 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #141 于 2026-09-19 基于最新 head `79b2179` 逐条验收 AC-01～AC-04 PASS 并合并（6 files，+199 / −444）：`gemini-embedding.provider.ts` 424 → 301 行，删手写重试环（`embedBatch` 循环、`sleepWithAbort`、`readRateLimitRetryDelayMs`、`withMetrics`、四个退避常量、`retrySleep` 注入、`EmbeddingError.retryAfterMs`），`retryOptions` 由 `{ attempts: 1 }` 改 `{ attempts: 3 }`（首次 + 2 次重试，退避用 SDK 默认 1s 起 ×2 带 jitter）；SDK 耗尽后可重试类错误（per-attempt timeout / 408 / 429 / 5xx）映射为 `retry_exhausted`，caller `embedding_retry_exhausted` 分支不变，daily quota 429 仍按 `rate_limit`；删 `EMBEDDING_BATCH_SIZE / EMBEDDING_REQUEST_TIMEOUT_MS / EMBEDDING_MAX_RETRIES` 三个 env、`readInteger / invalidConfig` 与 `.env.example` 三段，改为 `embedding-provider.ts` 常量 `EMBEDDING_BATCH_INPUTS` 64 / `EMBEDDING_ATTEMPT_TIMEOUT_MS` 60_000 / `EMBEDDING_RETRIES` 2，`EmbeddingRuntimeConfig` 收为 `{ apiKey, model, dimensions }`；boundary 测试经真实 SDK + `MockAgent`（client 级 `retryOptions.initialDelay` 缩到 1ms）证明首次 429 / 503 后成功、503 ×3 耗尽 transport 恰 3 次、连接失败 transport 1 次。读 SDK 源码（`@google/genai` 2.17.1 `ApiClient.apiCall` + p-retry 4.6.2）确认三处行为差异并记入 PR / Issue 决策记录：`Retry-After` 不被尊重（纯指数退避）；Node `TypeError: fetch failed` 不被 SDK 重试，Provider 直接按 `network` 上报（原有 2 次重试；2026-09-19 用户接受，#158 决策记录再次确认）；caller abort 落在 SDK 退避期间延迟 ≤ 4s（Run 路径已由 `tool-invocation.service` race，仅 CLI Ctrl-C 多等）。`retryCount` 字段保留恒为 0（删字段波及调用方，另议）。验证（基于 head 复跑）：api typecheck / lint，`test:article-indexing` 60/60、`test:retrieval` 34/34、`test:tools` 80/80，AC-01 grep 0，`index:articles -- --mode=full --source-id=19` 对真实 Gemini 成功（5 chunks、providerRequests 1）。与 Issue 措辞差异：`retryOptions` 只传 `attempts`（`initialDelay` / `maxDelay` 取 SDK 默认，3 次尝试下 `maxDelay` 不可能生效，且显式传值会覆盖测试用的 client 级注入）。`/code-review high` 3 条：修 1 条 Low（docstring），F1 Medium 网络错误不再重试与 F2 Low abort 延迟按 Issue「重试交给 SDK / 不手写重试」范围不修并记录。学习环节：待带读与独立改一处，通过后在 Issue 追加「学习已验证」 |
| #153 管理台概览与运行列表重构（健康 / 延迟 / 用量 / 工具） | Completed | 实施状态：已实现 / 验收状态：已通过。PR #160 代码 head `4e54a72` 的 AC-01～AC-08 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：概览接口改 SQL 聚合，只取必要 JSON 路径（健康：状态 / 成功率 / 按 errorCode 的失败原因；延迟：Run 时长 p50 / p95；用量：totalTokens 含 finalization attempts、缓存命中率与覆盖率；按 modelId 的模型行由服务端关联显示名；工具健康，不在清单的名字并为 unknown_tool）；模型调用口径（有 usage 或 llm_* 失败）在列表 / 详情 / 概览同源；运行列表加模型 / 失败原因列与 errorCode 筛选，概览失败原因可下钻；概览不再请求模型目录，余额中止竞态修复；`@agent/ai` grok outputTokens 归一为 completion + reasoning。验证：dev 库 30d 概览 = 模型表 = 列表 = 159,305；事务内构造数据验证后回滚；Playwright 验证模型目录 500、连续刷新余额与 1440px 布局；typecheck、api / admin / ai lint、`test:admin` 74、`@agent/admin test`、`@agent/ai` 55、`test:model-stream` 94、`test:grounding` 156、`test:agent-recorder` 21。`/code-review high` 两轮 18 条修 11 条。限制：24h 下钻按天筛会多出起点当天更早的 Run；中途中断且无 usage 的调用不计入；两个 Step 之间中断时列表无失败文案。2026-09-23 复核补记：#151 之前的失败调用没有 errorCode，无 usage 时既不计调用也不计失败；JSON 路径提取只省网络传输与 JS 解析，数据库侧仍整值解压 `output`；SQL 与 TS 两份口径实现只共用 `LLM_CALL_ERROR_CODES`，SQL 测试只断言语句文本。学习环节待做 |
| #152 Run 轨迹补齐模型可见内容 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #161 代码 head `c75119e` 的 AC-01～AC-07 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：tool Step 收口时与 output 同一事务落回喂参数（`toFeedbackArgumentsJson` 只算一次，同一字符串进 ModelContext）与 observation，未收口的 Step 两者都不写；采样 Step 落 Tool Call 轮 `intermediateText` / 非空 `reasoningContent`，`contextPlan` 加 `historyIncludedCount` / `observationPreviewChars`；finalization 落提示词三个标量；落库副本把 U+0000 与孤立代理项换成 U+FFFD；Admin Run Trace 纯文本展示（observation / reasoning 默认折叠），Retrieval 查询改读参数，「候选历史条数」标签。验证：`pnpm typecheck`、api / admin lint、`test:model-stream` 103、`test:context` 28、`test:grounding` 157、`test:admin` 75、`test:agent-recorder` 22、`test:tools` 80、`test:grounding-db` 15（补齐漏掉的 4 条 migration）、admin `test`、admin e2e 13；本机真实 DeepSeek 对话核对落库与 Admin 渲染。`/code-review xhigh` 两轮 25 条修 19 条。与 Issue 差异：reasoningContent 按「Tool Call 轮且非空」写，不按 compat。已知偏差写进根 `AGENTS.md` 第 6 节（历史只落条数、finalization 证据清单与草稿不落库）。2026-09-23 复核补记：U+0000 替换只覆盖 jsonb 副本，`Message.content` 与模型给的 toolName / callId 未处理，模型可见文本带 U+0000 会让 Run 停在 RUNNING（#167）；请求参数（`max_tokens` / 强度 / thinking）不落库，只能经 modelId 反查可被改动的模型行。学习环节待做 |
| #155 前台流式渲染与模型自恢复 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #162 代码 head `6dad027` 的 AC-01～AC-08 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：流式补齐以行、列表项、表格单元格为边界（同段落续行未闭合标记不补但可闭合），文末只有 `-` / `=` 的下划线行先不显示（含列表项、引用里的嵌套 setext），裸 URL 与后接空白的反引号不当标记起点；最后一个顶层块不读写缓存（随机文档前缀对比单测）；图片一律渲染成链接；流接口模型不可用的 400 抛 `ChatStreamHttpError`，发送被拒与打开下拉都重拉模型并纠正选中项、给出替换 / 读取失败提示，强度默认不发送；delta 按帧合并、原地更新不重排，会话轮次 v-for 加 v-memo（80 轮 + 400 delta 的 TaskDuration 1559→1091ms / 1094→490ms）；i18n localStorage 保护；只在 start / done 置顶；删无消费者返回值与孤儿 key；e2e 默认端口 5176。验证：web typecheck / lint / test 81 / build，不设 `E2E_PORT` 的 e2e 28 / 28，生产构建下 localStorage 抛错页面正常。`/code-review high` 三轮 26 条修 13 条。限制：模型不可用靠「400 且无 details」推断（显式错误码需改 API / contracts）；无前导竖线的表格、跨行粗体夹非 1 序号行等罕见输入的流式中间态仍可能多出字面标记。2026-09-23 复核补记：这类多出的标记会持续整行而非一帧，当前行里的字面 `*` 也会被补成强调，收尾在积压较多时超过 150ms（#169）；AC-05 的数字在 dev server 上测得；`useChatWorkspace.ts` 已 1,134 行，拆分按 Issue「不做」另议。学习环节待做 |
| #154 管理台模型接入页与小修 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #163 代码 head `a2570ba` 的 AC-01～AC-10 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：模型接入页 state 在家族 / 地址 / 密钥变了时重载模型表，弹窗测试结果按代次（AbortController）丢弃旧结果并中止旧请求，拉取 / 测试被中止时静默，首批失败时剩余批次一起记失败；模型弹窗置空家族不认的强度；服务端在 wireName / 强度 / maxOutput 或服务商家族 / 地址 / 密钥变更时清 `lastProbe*`，探活结果只在所用配置未变时条件写回；预算校验直接调 `resolveInitialContextBudget`，只在数值变化时校验；`LLMService` 探活 / 拉取 / 余额统一 30s 上界，`@agent/ai` 的 `listModels` / `getUserBalance` 接受 signal，403 文案带实际状态码；拉取模型 4xx / 非列表给带状态码的 `/v1` 提示且不回显上游 body；Retrieval Inspector 回退读 finalization input；Run 详情复制加 `execCommand` 回退；删 32 个孤儿 i18n key 并加引用检查；删 `refresh` / `keepStale` 与 run-api re-export；e2e 截图改到 `.artifacts`。验证：`pnpm typecheck`、admin / api / ai lint、`@agent/admin test`、`@agent/ai` 56、`test:admin` 76、`test:llm-config` 9、`test:model-stream` 103、`test:grounding` 157、admin e2e 13；本机 dev + fake 上游真实链路覆盖 AC-01～AC-09（挂住上游 30.1s 退出测试中、局域网 HTTP 复制、RUNNING finalization），临时数据已删。`/code-review high` 三轮 27 条修 10 条。限制：慢推理模型的已入库行探活可能被 30s 判为超时；预算校验不估算必带内容；客户端中止测试后服务端探活仍会跑完。2026-09-23 复核补记：预算只在数值变化时校验，已存在的预算 ≤ 0 行改名 / 切可见仍会放行（#170）；服务商事务持锁期间，探活条件写回按旧快照判断服务商条件，存在毫秒级竞态。学习环节待做 |
| #156 低成本安全加固 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #164 代码 head `1f4616d` 的 AC-01～AC-09 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：fetch / test-models 只带 providerId 时用库里 baseUrl，表单地址不同 400「更换地址需要重新填写 API Key」；PATCH 改地址须带新 key，地址与密钥只成对写入（只改备注不写回地址，并发下新 key 不配旧地址）；管理台编辑弹窗改地址时密钥必填、拉取置灰；`@agent/ai` 未映射状态码只报 `HTTP ${status}`，仅 JSON `error` 对象附带字符串 code / type 与去控制 / 格式字符、截断 200 字符的 message；余额只查 https 官方 DeepSeek 账号（跳过解不开的行）、删默认 Provider 回退、响应只投影声明字段；API 去 CORS、只监听 127.0.0.1，dev 端口探测与 Vite 代理目标同步 127.0.0.1；compose 主库 `127.0.0.1:5432`；`.env.example` 主密钥留空、补 `PORT`；GCM 标签固定 16 字节；测试 key 换假值。验证：`pnpm typecheck`、api / ai / admin / web lint、`@agent/ai` 61、`test:llm-config` 21（新增 fake prisma 单测）、`test:admin` 76、dev-apps 3、`test:db-reliability` 11、`test:grounding-db` 15；本机假上游 + 临时服务商真实故障注入 AC-01～AC-03（最终 head 复跑）、LAN 发消息 / 看 Run、外部 Origin 无 CORS 头、空主密钥启动失败，临时数据已删。`/code-review high` 三轮 25 条修 11 条。限制：局域网仍可经 5173 / 5174 代理调用全部接口（鉴权第 3 档）；本机主库容器按 #134 名字重建为 `agent-postgres`；运行中的 dev 代理目标重启后才换成 127.0.0.1。2026-09-23 复核补记：仓库默认的 Vite 只监听本机回环，局域网可达取决于启动时是否加 `--host`；其他机器上已有的主库容器要 `docker compose up -d` 重建才只绑 127.0.0.1（家里电脑已于当天重建）；API 无 Host 校验，DNS rebinding 可打到 127.0.0.1:3000，表单型简单 POST 不受 CORS 约束，均随鉴权到第 3 档；走明文 HTTP 的中转站每次 Run 都明文发送 key（余额已要求 https）；`listen` 写死 127.0.0.1，容器化时要改。学习环节待做 |
| #158 运行时小修 | Completed | 实施状态：已实现 / 验收状态：已通过。PR #166 代码 head `3223930` 的 AC-01～AC-06 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：eligible 工具只有参数通过校验才建立 Grounding Session 并登记结果（`invalid_arguments` 与截断同规则，先非法后命中为 `available`）；跨轮拼接用户可见文本保证隔一个空行，delta / done / 落库同源，也用于 Grounding 重放（Issue 字面只写非 Grounding，PR 已说明）；planner 删最旧历史落在问答中间时整对删；`test:tools` 排除 `*.db.test.ts`、`@agent/ai test` 用 find 匹配多层、根目录加 `test:dev-apps`、`test:llm-config` 先构建 contracts；usage 缓存字段只认有限数；`capabilitiesOf` 先 `Object.hasOwn`；修注释 / 测试标题漂移、删 `TokenEstimator.strategyId`、schema 只改注释。验证：`pnpm typecheck`、api / ai lint、`test:model-stream` 105、`test:context` 29、`test:grounding` 159、`@agent/ai` 67（5 个文件，深层探针验证后删除）、`test:llm-config` 22、`test:dev-apps` 3、带 DB 环境变量的 `test:tools` 80 且不含 db 用例；新 AC 用例在旧源码下均失败。`/code-review high` 两轮 14 条修 4 条。限制：候选读取触到硬上限时最旧一条仍可能是回答；eligible 工具只收到过非法参数时不建 Grounding Session，模型随后直接作答不经引用校验，与不调检索时一致（2026-09-23 复核补记）；其余 usage 计数字段、`DISPLAY_NAMES[wireName]` / admin `LLM_FAMILY_BRAND[family]` 的同类查找未改；测试脚本依赖 POSIX shell。学习环节待做 |
| #157 真实 Tool Call 流 fixture | Completed | 实施状态：已实现 / 验收状态：已通过。PR #165 代码 head `8d95252` 的 AC-01～AC-04 于 2026-09-23 逐条 PASS（PR 验收评论为证据）：一次性录制脚本 `record-tool-call-stream-fixtures.ts` 用生产 client 真实调用，deepseek 直连、中转站 gpt / grok、Google 官方直连 gemini（中转站 gemini 当天全线 upstream_error，用户同意改源，写入 Issue 决策记录）四份原始 SSE 经 SDK 回放，断言完整事件与 debug 捕获 call id 一致；累加器与 tee 共用 `appendToolCallIdentity`（相同 id / name 不再拼接）；gemini 缺 index、带调用报 stop，compat 加 `toolCallIndexOptional` / `toolCallsMayFinishWithStop` 只对 gemini 放宽。验证：`@agent/ai` 67、全仓 typecheck、api lint、`test:model-stream` 103、`test:llm-config` 21，字段改名变异 102 次中决定结果的字段全红。`/code-review high` 两轮 17 条修 8 条。限制：直连 Google 的 Gemini 3 续轮需回传 thought_signature（实测 400，未做），grounded finalization 也靠 Tool Call 提交，所以直连 Gemini 的「检索 → 有据回答」整条链路都不可用，中转站 gemini 的工具循环未验证过；同值连续的合法 id / name 分片（如 `foo` + `foo`）会被当成重复吞掉，Codex 在 PR #165 指出，按 AC-03 接受（四家真实流里 id / name 都只出现一次）（2026-09-23 复核补记）；gemini 思考 Token 不进 outputTokens（不改 usage 口径）。学习环节待做 |
| 内部数据工作台 | Planned | 合并 gsc：数据层迁入 → 页面移植 → agent 数据工具 → 容器与预览面板；档、触发、里程碑见 [workbench-direction](../research/workbench-direction.md) 第 7 节；一次只立一个 Issue |
| `web_fetch` 第一个真实工具 | Gated | 只读、SSRF 防护、untrusted observation，范围见 [pi-reference roadmap](../research/pi-reference/roadmap.md)。2026-09-19 转 Gated：用户确认当前没有会反复让 agent 读网页的真实用途；出现一个即 reopen 讨论 |
| Admin Console Task 4 | Planned | Auth / RBAC；触发条件见 [roadmap.md](../roadmap.md) 后置清单 |
| 已完成 | Completed | Phase 2–8、横向任务 #92 / #94 / #98 / #101–#104、Admin Console Task 0–3 与 Enhancement 1–3，归档在 [completed/](./completed/) |
| 翻译质检站 #109 / #111 | 已删除 | 2026-09-05 经 #113 / PR #114 删除全部代码与数据模型 |

## 状态定义

| 状态 | 含义 |
| --- | --- |
| Planned | 已记录方向或已建 Issue，但前置任务或启动条件尚未满足 |
| Next | 已确认是下一项正式任务，Issue 已创建但未开工 |
| Active | Issue 已创建，正在实现或待验收；一次只有一个 |
| Gated | 只有客观触发条件满足后才重新讨论 |
| 已放弃 / 已删除 | 方向放弃；代码保留或删除按看板记录 |
| Completed | 已实现且验收通过：PR 逐条验收 PASS 并合并 |

## 规则

- 一个 Issue 只对应一个任务单元；Planned / Next 不能替代正式 Issue；
- Issue 建立前不得修改正式代码（`docs/workflow.md` 第 2 节的小改动例外除外）；Issue 实质性变化后先更新 Issue 再继续；
- 实现后先写「已实现、待验收」，验收 PASS 后才写「已通过」；
- 只在真实使用卡住、源码阅读发现缺陷或缺口被明确命中时建 Issue；Admin Task 4、并行 Tool Call、Memory、MCP、Multi-agent 不自动进入实现；
- 流程、Issue 模板与任务状态定义见 [`../workflow.md`](../workflow.md)。
