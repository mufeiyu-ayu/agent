# 项目工作记录

本文件用于记录每次阶段性开发、提交或重要讨论的上下文，帮助 Codex 后续快速恢复项目记忆。

`docs/learning-log.md` 记录“学到了什么 Agent 概念”；本文件记录“项目推进了什么、这次 commit 或阶段完成了什么”。

## 当前项目状态

| 模块 | 当前状态 | 已完成 | 下一步 |
| --- | --- | --- | --- |
| 项目架构 | 进行中 | 已迁移为 `pnpm workspace`，拆分 `apps/api` 与 `apps/web` | 把 AI SEO 能力接入后端接口 |
| 后端 API | 已改为 SEO Agent 普通聊天接口 | NestJS demo 接口 `GET /api/demo` 可访问；已接入统一响应结构、全局校验管道、全局异常过滤器和 request id 中间件；LLM 配置统一从项目根目录 `.env` 读取；`LLMService` 保留模型调用、模型列表和余额查询；SEO 主链路已改为 `POST /api/seo/chat`，后端用 SEO Agent system prompt + user message 组织普通 chat，不再做固定字段 JSON Output 和 SSE | 下一步进入 T19：把最近对话 history 有控制地加入 messages |
| 前端 Web | 已改为简洁 SEO Agent 聊天工作台 | 接入 Tailwind CSS 与 Lucide，按设计稿重构 AI SEO Agent 单屏工作台；补齐 `components`、`views`、`hooks`、`types`、`utils`、`assets` 等前端基础目录；已将 `App.vue` 拆分为 `views`、`components`、`hooks`、`types`、`utils`；工作台已改为左侧平台 sidebar + 顶部状态栏 + 中央对话线程 + 底部 composer；顶部保留 DeepSeek 余额展示和刷新，底部 composer 保留文本输入、模型切换、发送和重置；已接入 `shadcn-vue` 基础组件库，复杂交互组件放入 `components/ui`，强定制业务布局继续保留 Tailwind 手写；已删除模板、关键词、语言、SEO 结果卡、trace 和 SSE 展示 | 下一步升级 T19：让后端收到最近几轮 user / assistant history |
| Agent 能力 | 学习迁移中 | 已学习 API 调用、多轮对话、JSON Output、Tool Calling、Streaming；已完成后端 `LLMService` 基础封装、真实模型调用、基础错误恢复、前端会话级记忆、模型运行时配置；当前重新收敛为 SEO Agent system prompt + 普通 user message 的自然聊天主链路 | 下一步练习简单上下文管理 |
| 项目任务计划 | 已优化 | 已建立 `docs/development-task-plan.md` 主任务看板，并按用户前端强、Nest 生疏、目标转 Agent 应用开发的能力曲线重排 T05-T20 和 Sprint 节奏；T05 至 T12、T14、T15、T17 已完成，T13 与 T16 暂缓 | 可进入 T20 产品收尾，也可继续 T18 / T19 Agent 能力练习 |

## 工作记录

| 日期 | 提交 | 类型 | 目标 | 核心完成 | 业务进度 | 讨论与决策 | 关键文件 | 验证结果 | 下一步 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 2026-05-28 | `edb34ee feat: 搭建前后端工作区骨架` | 架构迁移 | 从单文件 CLI 学习项目升级为前后端工程骨架 | 新增 `apps/api` Nest 后端、`apps/web` Vue/Vite 前端、axios 请求封装，删除旧根目录 `src/` 示例代码 | 前端已能通过 Vite 代理访问 Nest demo 接口 | 确认使用 Vue + Nest + pnpm workspace，根目录保留 Antfu ESLint；旧 `src/` 可删除，后续模型调用放到后端 | `pnpm-workspace.yaml`、`apps/api/src/app.controller.ts`、`apps/web/src/api/http.ts`、`apps/web/src/App.vue` | `pnpm typecheck`、`pnpm lint`、`pnpm --filter @agent/web build` 通过；`curl` 验证 `localhost:3000/api/demo` 与 `localhost:5173/api/demo` 均返回后端数据 | 在 Nest 中实现最小 AI SEO Agent 接口，并由 Vue 表单调用 |
| 2026-05-28 | `edb34ee feat: 搭建前后端工作区骨架` | 工作流规范 | 新增提交专用 skill，固定提交前后的项目记忆更新流程 | 创建 `.codex/skills/git-commit`，要求 commit 前更新 `docs/work-log.md`，必要时更新 `docs/learning-log.md`，再运行验证和提交 | 项目具备更稳定的 Codex 记忆维护流程 | 明确 skill 不能自动调用 skill，但可以在 `git-commit` 流程中规定 Codex 主动执行两个日志更新职责 | `.codex/skills/git-commit/SKILL.md`、`AGENTS.md`、`docs/work-log.md` | `pnpm lint`、`pnpm typecheck`、`pnpm --filter @agent/web build` 通过 | 下次用户要求 commit 时，使用该 skill 串起记录、验证和提交 |
| 2026-05-28 | `1195d6c fix: 修复 monorepo eslint 配置` | 工程修复 | 修复 Antfu ESLint 在 monorepo 子目录和 Vue 文件中不稳定生效的问题 | 根 ESLint 配置显式开启 `vue`，忽略所有子包 `dist`；给 `apps/api`、`apps/web` 补 `lint` / `lint:fix` 脚本；Cursor/VS Code 增加 `eslint.workingDirectories` 自动识别 | web 子包内的 Vue 文件现在能被根目录和子包 lint 同时检查 | 根因是 Antfu 在根目录运行时没有从 `apps/web/package.json` 自动推断 Vue 能力，导致 `eslint .` 漏掉 `.vue` 文件；monorepo 下应显式声明 Vue 支持和编辑器工作目录 | `eslint.config.mjs`、`.vscode/settings.json`、`apps/web/package.json`、`apps/api/package.json`、`apps/web/src/App.vue` | `pnpm lint`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/api lint`、`pnpm typecheck`、`pnpm --filter @agent/web build` 通过 | 后续提交前使用 `git-commit` skill 统一触发 lint、typecheck 和工作记录更新 |
| 2026-05-28 | `e7c47ad chore: 补充 node 版本约束` | 环境规范 | 补充根目录 Node.js 版本信息 | 新增 `.nvmrc` 固定推荐 Node `20.19.3`，在 `package.json.engines` 写入 Node 与 pnpm 约束，并在 README 补充快速开始前的环境要求 | 项目环境约束更明确，新成员或 Codex 后续进入项目时可先按版本切换环境 | 推荐版本选择 Node `20.19.3`，同时保留 Vite 兼容范围 `^20.19.0 \|\| >=22.12.0`，避免误把本机 Node `25.2.1` 当作项目标准 | `.nvmrc`、`package.json`、`README.md` | `pnpm lint`、`pnpm typecheck`、`pnpm --filter @agent/web build` 通过 | 后续提交前运行 lint、typecheck 和 web build |
| 2026-05-28 | `6ff02cc feat: 搭建前端工作台与开发约束` | 功能开发 | 按 UI 设计稿还原 AI SEO Agent 工作台，并调整为浏览器真实一屏布局 | 安装 `tailwindcss`、`@tailwindcss/vite`、`@lucide/vue`；接入 Tailwind Vite 插件和全局样式；将 demo 页面重构为带左侧侧边栏、顶部状态、输入表单、结果面板、SEO checks 和复制按钮的工作台；删除设计稿底部的状态预览、移动端预览和错误展示模块；去掉居中 `max-width` 画布限制，让工作台按真实浏览器宽度铺满 | 前端从 demo 接口展示页推进到 AI SEO 工具 UI 初版，后续只需把模拟生成逻辑替换为后端 SEO 接口调用 | 明确第一版产品页面不做设计稿说明区，保留真实工作台核心功能；桌面端使用 `h-screen`、`min-h-0` 和全宽 grid 控制一屏展示，面板内部在极端高度下自行滚动 | `apps/web/package.json`、`apps/web/vite.config.ts`、`apps/web/src/main.ts`、`apps/web/src/style.css`、`apps/web/src/App.vue` | `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build`、`pnpm typecheck`、`pnpm lint` 通过；内置浏览器当前无可用实例，未能自动截图验收 | 接入 `POST /api/seo/generate`，把前端模拟生成替换为真实 Nest + DeepSeek JSON Output 调用 |
| 2026-05-28 | `6ff02cc feat: 搭建前端工作台与开发约束` | 工程规范 | 建立前端目录基础和 Web 开发约束 skill，方便后续维护 | 新增 `apps/web/src/components`、`views`、`hooks`、`types`、`utils`、`assets` 目录；创建 `.codex/skills/web-frontend-development`，约束 Vue 组件拆分、Tailwind 真实尺寸适配、移动端响应式、主题切换、API / hooks / utils / types 分层，以及 `utils` 导出函数中文 TSDoc 规范；在 `AGENTS.md` 中加入使用该 skill 的触发说明 | 前端工程规范从口头约定沉淀为项目内可复用 skill，后续改 `apps/web` 时有明确约束 | 目录先用 `.gitkeep` 占位，不提前迁移当前 `App.vue`；等接入真实 SEO 接口或组件复杂度上升后再按规范拆分 | `AGENTS.md`、`.codex/skills/web-frontend-development/SKILL.md`、`.codex/skills/web-frontend-development/agents/openai.yaml`、`apps/web/src/components/.gitkeep`、`apps/web/src/views/.gitkeep`、`apps/web/src/hooks/.gitkeep`、`apps/web/src/types/.gitkeep`、`apps/web/src/utils/.gitkeep`、`apps/web/src/assets/.gitkeep` | `manual skill structure check passed`；官方 `quick_validate.py` 因当前 Python 环境缺少 `PyYAML` 未能运行，未安装额外 Python 包 | 下一步接入真实 SEO 接口时，将请求封装到 `api/seo.ts`，必要时把生成流程提取到 `hooks/useSeoGenerator.ts` |
| 2026-05-30 | `33984fc refactor: 拆分前端 SEO 工作台模块` | 重构 | 拆分 AI SEO 工作台前端单文件实现，降低 `App.vue` 维护成本 | `App.vue` 简化为视图入口；新增 `SeoWorkspaceView` 编排页面结构；拆分 `AppHeader`、`AppSidebar`、`SeoInputPanel`、`SeoStatusCard`、`SeoOrbitGraphic`、`SeoResultPanel`；将工作台状态迁移到 `useSeoWorkspace`，将 SEO 检查和时间格式化迁移到 `utils`，补齐 SEO 相关类型 | 前端工作台从单文件 demo 进入模块化结构，后续接入真实后端接口时可以在 hook 和 API 层逐步替换模拟逻辑 | 模块化拆分优先保持 UI 和交互行为不变；同时将模块化架构 skill 的触发范围从单个文件扩大到 `apps/web/**` 和 `apps/api/**`，避免后续继续堆大文件 | `apps/web/src/App.vue`、`apps/web/src/views/SeoWorkspaceView.vue`、`apps/web/src/components/layout/AppHeader.vue`、`apps/web/src/components/layout/AppSidebar.vue`、`apps/web/src/components/seo/SeoInputPanel.vue`、`apps/web/src/components/seo/SeoResultPanel.vue`、`apps/web/src/components/seo/SeoStatusCard.vue`、`apps/web/src/components/seo/SeoOrbitGraphic.vue`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/types/seo.ts`、`apps/web/src/utils/seo-check.ts`、`.codex/skills/modular-architecture-development/SKILL.md` | `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm typecheck`、`pnpm lint`、`pnpm --filter @agent/web build` 通过；`curl -I http://localhost:5174/` 返回 `200 OK`；内置浏览器列表为空，未能完成截图验收 | 接入 `POST /api/seo/generate`，把 `useSeoWorkspace` 中的模拟生成逻辑替换为真实后端调用 |
| 2026-05-30 | `a332edd feat: 配置 Nest 后端基础设施` | 功能开发 | 为 Nest 后端补齐基础运行环境，统一 HTTP 边界行为 | 新增 `common` 基础设施：统一成功响应 interceptor、全局异常 filter、全局 validation pipe、request id middleware 和 bootstrap 注册函数；补充 `class-validator` / `class-transformer`；前端 axios 响应拦截器自动解包统一响应，保持现有业务 API 返回类型不变 | 后端从 demo controller 进入可扩展基础设施阶段，后续新增 SEO 接口时可以直接复用统一响应、校验和错误结构 | 统一响应放在 interceptor，异常格式放在 filter，请求校验放在 pipe，中间件只负责请求链路元数据；没有引入复杂框架，保持小步可运行 | `apps/api/src/common/**`、`apps/api/src/main.ts`、`apps/api/src/app.module.ts`、`apps/web/src/api/http.ts`、`apps/api/package.json` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/api lint`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build`、`pnpm typecheck`、`pnpm lint` 通过；`PORT=3101` 临时启动后 `curl /health` 和 `curl /not-found` 验证统一成功/错误响应通过 | 新增 `POST /api/seo/generate` DTO、Service 和最小 JSON Output 调用 |
| 2026-05-30 | 待提交 | 技术决策 | 收紧学习日志和提交流程边界，避免学习记录混入普通项目进度 | 清理 `docs/learning-log.md` 中的工程搭建、工具迁移和后端基础设施记录；更新 Codex / Claude 学习日志 skill，明确只记录 Agent 概念学习；更新 `git-commit` skill，要求 `learning-log.md` 不和普通业务代码默认混在同一个提交 | 项目文档职责更清晰，学习日志用于 Agent 概念复盘，工作记录用于项目推进和提交上下文 | 确认默认工程搭建、UI 调整、依赖安装、commit 上下文、普通后端基础设施不进入学习日志；只有 LLM、messages、prompt、JSON Output、streaming、Tool Calling 等 Agent 概念学习才记录 | `docs/learning-log.md`、`.codex/skills/update-agent-learning-log/SKILL.md`、`.claude/skills/update-agent-learning-log/SKILL.md`、`.codex/skills/git-commit/SKILL.md`、`.codex/skills/update-agent-learning-log/agents/openai.yaml` | 未运行代码验证；本次只调整 Markdown 文档和 skill 规则 | 后续提交时按新规则分离学习日志与业务代码提交 |
| 2026-05-30 | 待提交 | 工程配置 | 为前端配置 `@` 路径别名到 `apps/web/src` | 在 Vite `resolve.alias` 中配置 `@` 指向 `src`，在 `tsconfig.json` 中配置 `@/*` 到 `./src/*`，并将 `main.ts` 的入口 import 改为 `@/...` 验证别名生效 | 前端后续组件、hooks、api、types 引用可以使用稳定的 `@` 绝对路径 | TypeScript 6 下 `baseUrl` 会触发弃用错误，因此使用不含 `baseUrl` 的相对 `paths` 写法 | `apps/web/vite.config.ts`、`apps/web/tsconfig.json`、`apps/web/src/main.ts` | `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build` 通过 | 后续重构前端 import 时可逐步改成 `@/...` |
| 2026-05-30 | `d9db50d docs: 建立项目任务计划` | 技术决策 | 建立项目完成任务主看板，后续开发以任务计划为基础推进 | 新增 `docs/development-task-plan.md`，按产品定义、前端工作台、后端接口、LLM 调用、JSON Output、SEO 检查、联调、错误恢复、Streaming、Tool Calling、评估和收尾整理项目完成任务；更新 `git-commit` skill、`AGENTS.md` 和 `README.md`，要求后续开发优先以任务计划为基线 | 项目后续推进有统一任务基线，不再只依赖工作日志里的“下一步”字段 | 任务计划只记录业务相关事项，不记录环境、lint、格式化、提交流程等杂项；`work-log.md` 记录发生过什么，`development-task-plan.md` 记录完成路径和状态 | `docs/development-task-plan.md`、`.codex/skills/git-commit/SKILL.md`、`AGENTS.md`、`README.md`、`docs/work-log.md` | 未运行代码验证；本次只新增和调整 Markdown 文档 | 下一步按 T05 实现 `POST /api/seo/generate` |
| 2026-05-30 | `1ff3c4b docs: 优化 AI SEO Agent 开发任务计划` | 技术决策 | 按用户当前能力重新优化 AI SEO Agent 开发任务计划 | 将 T05-T20 重排为更细的任务链路，新增计划设计原则、当前主线和 Sprint 1-6；明确先做 SEO 接口骨架和本地 checks，再做前端真实调用，最后接 LLMService、JSON Output 和端到端闭环 | 项目后续开发节奏更贴合“前端强、Nest 生疏、目标转 Agent 应用开发”的能力曲线，降低一次性同时学习 Nest、LLM、JSON Output 和前端联调的负担 | 确认当前不继续优化 UI，也不提前做 RAG、多 Agent、数据库、Streaming；第一阶段只做 Nest SEO 模块、DTO、Controller、Service 和本地 SEO 检查工具 | `docs/development-task-plan.md`、`docs/work-log.md` | 未运行代码验证；本次只调整 Markdown 文档 | 按 Sprint 1 开始实现 T05 和 T07 最小版本：SEO 接口骨架 + 后端本地 SEO checks |
| 2026-05-30 | `c367666 feat: 接入 SEO 生成接口联调` | 功能开发 | 完成 Sprint 1：新增后端 SEO 生成接口骨架和本地 SEO checks | 新增 `seo` 模块、DTO、Controller、Service、types、tools 和模块内 utils；`POST /api/seo/generate` 接收页面主题、语言、关键词，返回 mock `title` / `description` / `checks`；修复 Nest ESM 下 provider 注入和 DTO 校验依赖运行时元数据的问题；当前阶段 checks 收敛为 title/description 是否生成、关键词是否命中 | T05 与 T07 最小版本完成，后端已具备前端联调所需接口 | 保持 Controller 只处理 HTTP 边界，Service 编排流程，Tool 负责确定性检查，utils 负责当前 SEO 模块独享的纯辅助逻辑；当前 mock 阶段不做复杂 SEO 长度评价，避免把真实 SEO 策略过早写死 | `apps/api/src/seo/**`、`apps/api/src/app.module.ts`、`apps/api/src/main.ts`、`apps/api/src/common/pipes/app-validation.pipe.ts`、`docs/development-task-plan.md` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/api lint` 通过；`PORT=3001 pnpm --filter @agent/api dev` 后用 `curl` 验证成功请求返回 SEO 结果，空 `pageTopic` 和多余字段返回 400；中文样例 title、description、keywords 三个 checks 均 pass | 按 Sprint 2 新增前端 `api/seo.ts`，把 `useSeoWorkspace` 的 `setTimeout` mock 替换为真实后端调用 |
| 2026-05-30 | `c367666 feat: 接入 SEO 生成接口联调` | 接口联调 | 完成 Sprint 2：前端真实调用后端 SEO 接口 | 新增 `apps/web/src/api/seo.ts` 和 SEO 请求/响应类型；`useSeoWorkspace` 移除 `setTimeout` mock，改为调用 `POST /api/seo/generate`；前端使用后端返回的 `title`、`description`、`checks` 和 `generatedAt` 更新页面状态；Vite 代理目标支持通过 `VITE_API_PROXY_TARGET` 覆盖 | T06 完成，Vue -> Vite proxy -> Nest SEO mock 接口链路已跑通 | 不在前端保存模型密钥；前端只关心业务响应和 loading/error/success 状态；因本机 `3000`、`5173` 已有旧进程，本次联调用 `3001` 和 `5174` 避免干扰 | `apps/web/src/api/seo.ts`、`apps/web/src/api/http.ts`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/types/seo.ts`、`apps/web/vite.config.ts`、`docs/development-task-plan.md` | `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build`、`pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/api lint` 通过；`curl http://localhost:5174/api/seo/generate` 成功经前端代理返回后端 SEO 结果，空 `pageTopic` 返回 400 | 按 Sprint 3 开始后端 `LLMService`，用 DeepSeek / OpenAI-compatible JSON Output 替换 mock 生成 |
| 2026-05-30 | `1ff667c fix: 补齐 LLMService 环境变量加载` | 工程修复 | 补齐 LLMService 环境变量加载和配置一致性 | 后端 dev 启动统一加载项目根目录 `.env`；根目录 env 示例统一为 `LLM_*`；删除 API 子目录 env 示例文件，避免出现两套 env 示例；`LLMService` 显式校验 `LLM_API_KEY`、`LLM_BASE_URL`、`LLM_MODEL`；规范化 `LLM_BASE_URL` 尾斜杠，避免拼出 `//chat/completions` | T08 配置补丁完成，T09 尚未开始 | 根据用户要求只保留项目根目录 `.env` 作为运行配置来源；未接 `SeoService`，未做 JSON Output，未改前端 | `apps/api/package.json`、`apps/api/src/llm/llm.service.ts`、`.env.example`、`docs/development-task-plan.md`、`docs/work-log.md` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/api lint`、`pnpm typecheck`、`pnpm lint` 通过；`PORT=3999 pnpm --filter @agent/api dev` 可启动并读取根目录 env 路径 | 进入 T09：JSON Output 落地 |
| 2026-05-31 | `6632568 feat: 封装 SEO JSON Output 生成服务` | 功能开发 | 开始 T09：准备 SEO JSON Output 的 prompt 和运行时校验 | 新增 SEO 生成 prompt 构造函数；新增模型 JSON 输出解析与字段校验；补充 `SeoGenerationOutput` 和 `SeoGenerationOutputError` 类型 | T09 进入进行中，当前只准备 JSON Output 能力，尚未接入 `SeoService` | 按“小步可运行”节奏先把模型输出协议和 validator 单独落地；本次不替换 mock，不改前端，不做 Streaming | `apps/api/src/seo/prompts/seo-generation.prompt.ts`、`apps/api/src/seo/validators/seo-output.validator.ts`、`apps/api/src/seo/types/seo.types.ts`、`docs/development-task-plan.md`、`docs/learning-log.md` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/api lint`、`pnpm typecheck`、`pnpm lint` 通过 | 下一步用 `LLMService` 发起一次 JSON Output 调用，并验证 validator 对合法/非法输出的处理 |
| 2026-05-31 | `6632568 feat: 封装 SEO JSON Output 生成服务` | 功能开发 | 补齐 T09：封装 SEO 场景的 JSON Output 调用服务 | 新增 `SeoGenerationService`，在 SEO 模块内组合 prompt、`LLMService.chat` 和 output validator；在 `SeoModule` 注册 provider | T09 的模型生成能力已有独立服务承载，当前接口仍保持 mock，T10 再接入业务编排 | 确认正式架构应由 SEO 模块调用通用 LLM 模块，而不是把 SEO 逻辑写进 `main.ts` 或验证脚本；本次不改 `SeoService`、不改前端 | `apps/api/src/seo/seo-generation.service.ts`、`apps/api/src/seo/seo.module.ts`、`docs/development-task-plan.md`、`docs/work-log.md` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/api lint`、`pnpm typecheck`、`pnpm lint` 通过 | 下一步进入 T10：在 `SeoService` 中接入 `SeoGenerationService`，并保留 SEO checks 后处理 |
| 2026-05-31 | `b3e2436 feat: 接入真实 SEO 生成并补充后端 watch` | 功能开发 | 完成 T10：将 SEO 正式接口从 mock 切到真实 LLM 生成 | `SeoService` 注入 `SeoGenerationService`，调用 LLM JSON Output 生成 `title` / `description`，再复用 `buildSeoChecks` 输出本地检查结果 | `/api/seo/generate` 已进入真实模型生成链路，前端现有请求路径不变 | 不新增 preview endpoint；保持 Controller HTTP 边界稳定；本次不改前端、不做 Streaming、不做 Tool Calling | `apps/api/src/seo/seo.service.ts`、`docs/development-task-plan.md`、`docs/work-log.md`、`docs/learning-log.md` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/api lint`、`pnpm typecheck`、`pnpm lint` 通过；`curl POST /api/seo/generate` 固定样例返回模型生成的 `title`、`description`、3 个 checks 和 `requestId` | 进入 T11：Vue -> Nest -> LLM -> SEO Tool -> Vue 最小闭环联调 |
| 2026-05-31 | `b3e2436 feat: 接入真实 SEO 生成并补充后端 watch` | 工程配置 | 增加服务端 watch 启动脚本 | API 子包新增 `dev:watch`，根目录新增 `dev:api:watch` 快捷命令，后端代码变更后可自动重启 | 开发体验优化，不改变业务功能 | 继续复用根目录 `.env` 加载方式；本次不新增依赖，不改前端 | `package.json`、`apps/api/package.json`、`docs/work-log.md` | `PORT=3997 pnpm dev:api:watch` 可启动并进入 watch 模式，手动 `Ctrl+C` 停止；`pnpm typecheck`、`pnpm lint` 通过 | 后续联调可用 `pnpm dev:api:watch` 启动后端 |
| 2026-05-31 | `6267db3 feat: 补齐生成错误提示与前端校验` | 功能开发 | 补齐前端生成前的表单内联校验 | 点击 Generate 时校验 Page Topic 必填、Target Keywords 至少 1 个；错误在对应表单元素下方以红色文字展示；用户继续输入或添加关键词后自动清理对应错误 | T13 输入边界进入进行中，先完成前端必填拦截，避免空输入请求后端和模型 | 不使用弹窗；校验状态放在 `useSeoWorkspace`，`SeoInputPanel` 只负责展示错误；本次不改后端、不改接口 | `apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/components/seo/SeoInputPanel.vue`、`apps/web/src/views/SeoWorkspaceView.vue`、`apps/web/src/types/seo.ts`、`docs/development-task-plan.md`、`docs/work-log.md` | `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build`、`pnpm typecheck`、`pnpm lint` 通过 | 用户自行浏览器验证；下一步对齐前后端输入长度、关键词数量和成本控制规则 |
| 2026-05-31 | `6267db3 feat: 补齐生成错误提示与前端校验` | 功能开发 | 开始 T12：补齐模型失败的后端错误映射和前端 message 提示 | 全局异常过滤器识别 `LLMError` 与 `SeoGenerationOutputError`，映射为安全的 HTTP message；前端新增轻量 `AppMessage`，生成失败时展示后端错误 message；生成请求增加请求锁和 800ms 节流；移除模型原始输出调试日志 | T12 进入进行中，坏 key 场景已能从后端返回可读错误，并由前端具备弹窗展示入口 | 暂不引入第三方 toast 包；先保证错误语义正确，再用项目内组件展示；错误响应保留 `requestId`，不返回 API Key 或原始模型错误详情 | `apps/api/src/common/filters/all-exceptions.filter.ts`、`apps/api/src/seo/seo-generation.service.ts`、`apps/web/src/components/common/AppMessage.vue`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/views/SeoWorkspaceView.vue`、`apps/web/src/types/seo.ts` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/api lint`、`pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build`、`pnpm typecheck`、`pnpm lint` 通过；`LLM_API_KEY=bad-key PORT=3002 pnpm dev:api:watch` 后 `curl POST /api/seo/generate` 返回 502 和 `AI 服务认证失败，请检查服务端模型配置` | 继续补充网络失败、限流和 JSON 格式异常样例；用户可在浏览器验证 message 展示和重复点击拦截 |
| 2026-05-31 | `0d526b1 fix: 收口 T12 基础错误恢复` | 接口联调 | 完成 T11：前端到真实 LLM 的最小闭环联调 | 使用前端页面完成 Vue -> Nest -> LLM -> JSON validator -> SEO checks -> Vue 展示链路验证；正常生成、前端必填校验和坏 key 错误提示均已由用户手动验证 | T11 已完成，项目进入错误恢复和输入边界完善阶段 | 以用户手动浏览器验收为准，本次只同步任务状态，不改代码 | `docs/development-task-plan.md`、`docs/work-log.md` | 未运行代码验证；本次仅更新文档状态 | 继续推进 T12 剩余失败样例和 T13 前后端输入规则对齐 |
| 2026-05-31 | `0d526b1 fix: 收口 T12 基础错误恢复` | 工程修复 | 收口 T12：优化基础错误恢复实现 | 后端异常日志补充 `requestId`、请求路径和状态码；AI 相关 4xx 错误也会写入 warn 日志便于定位；`LLMService` 为模型请求增加 10 秒超时，并将成功响应 JSON 解析失败包装为安全的 LLM 错误；前端生成失败时读取 `error.details` 改为安全可选链 | T12 已完成，错误恢复不再继续扩张状态码体系，下一步集中做 T13 输入边界 | 保留现有 LLM 错误类型和全局异常映射，不新增依赖、不引入第三方 toast；失败提示继续以安全 message 返回给前端，详细错误只留在服务端日志 | `apps/api/src/common/filters/all-exceptions.filter.ts`、`apps/api/src/llm/llm.service.ts`、`apps/web/src/hooks/useSeoWorkspace.ts`、`docs/development-task-plan.md`、`docs/work-log.md` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/api lint`、`pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build` 通过 | 进入 T13：对齐前后端页面主题长度、关键词数量、关键词长度和成本控制规则 |
| 2026-05-31 | `9a32a72 docs: 暂缓 T13 并准备 T16 评估` | 技术决策 | 暂缓 T13 输入边界细化，调整下一步为 T16 基础效果评估 | 将 T13 标记为暂缓，只保留已完成的必填校验；将 T16 标记为准备中，下一步先整理固定输入样例和人工评估检查点 | 项目从继续加输入限制，调整为先观察和评估真实模型生成质量 | 当前产品输入规则后续可能变化，过早限制长度、关键词数量和成本规则容易返工；评估样例对后续 prompt、JSON Output 和结果体验更有复用价值 | `docs/development-task-plan.md`、`docs/work-log.md` | 未运行代码验证；本次只调整任务计划和工作记录 | 展示 T16 的最小实施方案，并在确认后再写入具体评估样例 |
| 2026-06-03 | `8e3923e docs: 暂缓 T16 基础效果评估` | 技术决策 | 将 T16 从第一版主线中移出 | 按用户判断把 T16 基础效果评估标记为暂缓，并从 Sprint 5 中移除；后续第一版增强聚焦 T14 优化建议和 T15 会话级历史 | 项目不再把固定评估样例作为当前阻塞任务，下一步切到 T14 | 当前学习阶段更适合继续补产品可见能力，评估样例后续有 prompt 回归需求时再恢复 | `docs/development-task-plan.md`、`docs/work-log.md` | 未运行代码验证；本次只调整 Markdown 文档 | 进入 T14：扩展 SEO JSON Output，让接口返回 3 到 5 条优化建议并在前端展示 |
| 2026-06-03 | `1acf831 feat: 增加 SEO 优化建议` | 功能开发 | 完成 T14 结果优化建议并移除 SEO Checks | 后端 prompt、JSON validator 和响应类型新增 `suggestions`；`SeoService` 不再返回 `checks`；删除后端 SEO check tool 和前端 checks 展示；前端结果面板展示 3 到 5 条优化建议 | T14 已完成，第一版结果区聚焦 title、description 和 suggestions | 用户确认 SEO Checks 对当前产品价值不高，因此从第一版接口和 UI 中移除；后续 Tool Calling 将重新设计更有价值的工具场景 | `apps/api/src/seo/**`、`apps/web/src/components/seo/SeoResultPanel.vue`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/types/seo.ts`、`apps/web/src/utils/seo-format.ts`、`docs/development-task-plan.md`、`docs/learning-log.md`、`docs/work-log.md` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/api lint`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build`、`pnpm typecheck`、`pnpm lint` 通过；`curl POST /api/seo/generate` 固定样例返回 5 条 suggestions 且无 checks 字段 | 进入 T15：前端保存当前会话内最近生成结果 |
| 2026-06-03 | `c3da6e4 feat: 改造 Results 为会话线程` | 功能开发 | 完成 T15 对话式 Results 和会话级记录 | 新增 `SeoConversationTurn` 类型；`useSeoWorkspace` 保存最近 8 个 user / agent turn；Results 面板改为对话线程展示；新增 `SeoConversationTurn` 组件承载单条对话展示；复制状态按 turn + 字段区分 | T15 已完成，当前只做刷新即丢失的前端会话记忆，不接数据库，不把历史传给模型 | 将“记忆”拆成前端会话记忆、模型上下文记忆和长期持久记忆三层；本次先做第一层，为 T17 流式事件和 T19 上下文管理打基础 | `apps/web/src/types/seo.ts`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/components/seo/SeoResultPanel.vue`、`apps/web/src/components/seo/SeoConversationTurn.vue`、`apps/web/src/views/SeoWorkspaceView.vue`、`docs/development-task-plan.md`、`docs/work-log.md`、`docs/learning-log.md` | `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build`、`pnpm typecheck`、`pnpm lint` 通过；`curl POST /api/seo/generate` 固定样例成功；`curl --noproxy '*' -I http://localhost:5173/` 返回 200；未做浏览器截图验收 | 进入 T17：用 SSE 或流式协议把生成过程状态接入 conversation turn |
| 2026-06-03 | `4aa2465 feat: 接入 SEO 流式生成体验` | 功能开发 | 完成 T17：把生成过程状态以 SSE 接入 Results 当前 turn | 后端新增 `SeoStreamService` 和 `POST /api/seo/generate/stream`；Controller 用 `text/event-stream` 输出 `started`、`progress`、`result`、`done` / `error` 事件；前端新增 `streamGenerateSeoContent`，用 `fetch + ReadableStream` 解析 SSE；`useSeoWorkspace` 将流式事件写入当前 conversation turn 的 `progressMessage`、`result` 和错误状态；新增前端阶段队列，收到 `result` 后先缓存结果，展示 `Preparing final answer` 后再渲染最终内容 | T17 已完成，用户能在等待模型生成时看到阶段进度，最终结构化结果仍展示在同一个 Agent 盒子中，且不会在模型返回瞬间突兀切换 | 当前版本先做事件级 streaming，不直接做模型 token-by-token 输出；非流式 `POST /api/seo/generate` 保留，方便兼容和调试；展示节奏放前端处理，避免后端为了 UI 体验引入人为等待 | `apps/api/src/seo/seo.controller.ts`、`apps/api/src/seo/seo-stream.service.ts`、`apps/api/src/seo/types/seo-stream.types.ts`、`apps/api/src/seo/seo.module.ts`、`apps/web/src/api/seo.ts`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/types/seo.ts`、`apps/web/src/components/seo/SeoConversationTurn.vue`、`docs/development-task-plan.md`、`docs/work-log.md`、`docs/learning-log.md` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/api lint`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build`、`pnpm typecheck`、`pnpm lint` 通过；追加验证 `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build` 通过；`PORT=3100 pnpm --filter @agent/api dev` 临时启动后，`curl --noproxy '*' -N POST /api/seo/generate/stream` 返回 `started`、`progress`、`result`、`done`；非流式 `/api/seo/generate` 同样返回成功；`curl --noproxy '*' -I http://localhost:5173/` 返回 200；内置 Browser 工具未暴露可用调用，未做截图验收 | 可选择 T20 第一版产品收尾，也可继续 T18 Tool Calling 或 T19 上下文管理 |
| 2026-06-03 | `f3c4f75 feat: 完善 Agent 平台工作台` | 前端重构 | 将 SEO 工作台重构为 Agent 平台式对话界面 | 新增 `AppShell` 承载全屏平台骨架；`AppSidebar` 和 `AppHeader` 通过 `useMockAgentPlatform` 接收 mock 平台数据；新增 `agent` 通用组件承载对话线程、消息外壳、生成步骤卡和 trace；新增 `SeoResultCard`、`SeoTemplatePanel`、`SeoContextTags` 和 `SeoChatComposer`，把 SEO 结果、模板上下文和底部输入拆开；删除旧的 `SeoInputPanel`、`SeoConversationTurn`、`SeoResultPanel`、`SeoStatusCard`、`SeoOrbitGraphic` | T19 前置 UI 形态已准备好，后续可以把请求结构升级为 `instruction + context + history` | 当前不引入第三方 chat UI，不改后端接口；平台 sidebar / recent chats / credits 允许先用 mock 数据，业务结果区只展示真实返回字段和可计算长度信息 | `apps/web/src/components/layout/AppShell.vue`、`apps/web/src/components/layout/AppSidebar.vue`、`apps/web/src/components/layout/AppHeader.vue`、`apps/web/src/components/agent/*`、`apps/web/src/components/seo/SeoResultCard.vue`、`apps/web/src/components/seo/SeoChatComposer.vue`、`apps/web/src/components/seo/SeoTemplatePanel.vue`、`apps/web/src/components/seo/SeoContextTags.vue`、`apps/web/src/hooks/useMockAgentPlatform.ts`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/types/agent-platform.ts`、`apps/web/src/types/seo.ts`、`apps/web/src/views/SeoWorkspaceView.vue`、`docs/development-task-plan.md`、`docs/work-log.md` | `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build` 通过；`curl --noproxy '*' -I http://localhost:5173/` 返回 200；内置 Browser 工具未暴露可用调用，未做截图验收 | 进入 T19：后端请求结构升级为 `instruction + context + history`，并限制传给模型的最近对话轮数 |
| 2026-06-04 | `f3c4f75 feat: 完善 Agent 平台工作台` | 功能开发 | 为 Agent 平台补 DeepSeek 运行时设置区 | 后端新增 `GET /api/llm/models` 和 `GET /api/llm/balance` 代理 DeepSeek 官方接口；前端新增 `api/llm.ts`、`types/llm.ts` 和 `useLlmRuntime` 管理模型列表、当前模型、余额和刷新状态；侧边栏移除 `Upgrade Plan` 卡片；顶部右侧显示余额、刷新按钮和语言选择；底部 composer 增加 DeepSeek 模型切换；生成请求会携带当前模型；全局字体调整为更接近系统网页端的舒适字体栈 | T19 前置平台运行时能力补齐，后续进入真正的 `instruction + context + history` 消息结构 | 模型选择只允许当前 DeepSeek 官方列表中的 `deepseek-v4-flash` 和 `deepseek-v4-pro`，前端不接触 API Key；余额和模型列表都由 Nest 后端代理，避免把模型平台认证暴露到浏览器 | `apps/api/src/llm/llm.controller.ts`、`apps/api/src/llm/llm.service.ts`、`apps/api/src/llm/llm.types.ts`、`apps/api/src/seo/dto/generate-seo.dto.ts`、`apps/api/src/seo/seo-generation.service.ts`、`apps/web/src/api/llm.ts`、`apps/web/src/hooks/useLlmRuntime.ts`、`apps/web/src/types/llm.ts`、`apps/web/src/components/layout/AppHeader.vue`、`apps/web/src/components/layout/AppSidebar.vue`、`apps/web/src/components/seo/SeoChatComposer.vue`、`apps/web/src/style.css` | `pnpm --filter @agent/api typecheck`、`pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/api lint`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build` 通过；`PORT=3100 pnpm --filter @agent/api dev` 临时启动后验证 `/api/llm/models` 和 `/api/llm/balance` 成功；当前 3000 服务同样能返回两个新接口；`curl --noproxy '*' -I http://localhost:5173/` 返回 200 | 继续 T19：把自由输入、模板上下文和最近对话 turn 组织成后端 messages，控制传入模型的历史轮数 |
| 2026-06-04 | `f3c4f75 feat: 完善 Agent 平台工作台` | 前端优化 | 优化 Agent 工作台 sidebar 和主区域视觉 | `AppShell` 增加桌面 sidebar 折叠状态和移动端抽屉状态；`AppSidebar` 根据 `collapsed` 展示展开/收起两套导航形态，并支持移动端关闭；`AppHeader` 的移动端菜单按钮接入抽屉打开；主应用、body 和 composer 背景调整为更干净的白色层次；移除底部 composer 顶部分界线 | Agent 平台壳进一步接近真实产品工作台，侧边栏可收纳，中心区域视觉更轻 | 折叠/抽屉属于 layout 状态，放在 `AppShell`；SEO 业务组件不参与导航状态；本次不引入第三方抽屉库，继续用 Vue 状态和 Tailwind 完成 | `apps/web/src/components/layout/AppShell.vue`、`apps/web/src/components/layout/AppSidebar.vue`、`apps/web/src/components/layout/AppHeader.vue`、`apps/web/src/components/seo/SeoChatComposer.vue`、`apps/web/src/style.css` | `pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web lint`、`pnpm --filter @agent/web build` 通过；`curl --noproxy '*' -I http://localhost:5173/` 返回 200；当前会话未暴露浏览器自动化工具，未做截图验收 | 继续在真实浏览器里检查折叠态、移动端抽屉和底部输入区视觉，再进入 T19 消息结构升级 |
| 2026-06-15 | `fb4152a refactor: 简化 SEO Agent 聊天链路` | 架构调整 | 将 SEO 固定字段生成器重构为普通 SEO Agent 聊天助手 | 后端删除固定字段 DTO、JSON Output prompt/validator、`SeoGenerationService`、`SeoStreamService` 和 SSE endpoint；新增 `SeoChatDto`、SEO Agent system prompt 和 `POST /api/seo/chat`；前端删除模板、关键词、语言、SEO 结果卡、trace 和 SSE 消费逻辑，composer 只保留文本输入、模型选择、发送、重置，conversation 只展示 user / assistant 普通文本 turn | T19 进入进行中，已跑通最小 `system + user` 单轮 SEO Agent chat | 当前不做字段抽取、不做 JSON Output、不做 SSE；先把 SEO Agent 角色和自然对话主链路稳定下来，再逐步加入 history、工具调用和结构化能力 | `apps/api/src/seo/**`、`apps/api/src/common/filters/all-exceptions.filter.ts`、`apps/web/src/api/seo.ts`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/web/src/types/seo.ts`、`apps/web/src/components/agent/AgentConversation.vue`、`apps/web/src/components/seo/SeoChatComposer.vue`、`apps/web/src/views/SeoWorkspaceView.vue`、`docs/development-task-plan.md`、`docs/work-log.md`、`docs/learning-log.md` | `pnpm typecheck`、`pnpm lint`、`pnpm --filter @agent/web build` 通过；`curl --noproxy '*' -X POST http://localhost:3000/api/seo/chat` 成功返回普通文本 `reply`；服务端 watch 已自动重启并只注册 `/api/seo/chat` | 下一步在 `SeoChatRequest` 中加入最近几轮 history，并在后端控制传入模型的上下文长度 |
| 2026-06-16 | 待提交 | 前端工程化 | 接入 `shadcn-vue` 组件库并收敛使用边界 | 新增 `components.json`、`components/ui` 和 `lib/utils`；按需生成 `Button`、`Textarea`、`Select`、`Alert`、`Avatar`、`Sheet`、`ScrollArea` 等基础组件；底部 composer 使用 `Textarea` / `Select` / `Button`，移动端侧边栏抽屉使用 `Sheet`，提示条使用 `Alert`；侧边栏导航和空状态建议 chip 回退为手写 Tailwind，避免组件库默认样式破坏业务布局；输入框默认值改为空，仅保留浅灰 placeholder；`AGENTS.md` 补充组件库使用边界；SEO Agent system prompt 补充“贾维斯”身份设定 | 前端组件库底座已建立，但使用范围收敛为复杂交互组件，不强行替换强定制业务布局 | 组件库不是为了减少每一行模板代码，而是承接复杂交互和无障碍基础；普通导航、业务卡片、简单标签继续用 Tailwind 更可控 | `AGENTS.md`、`apps/web/components.json`、`apps/web/src/components/ui/**`、`apps/web/src/lib/utils.ts`、`apps/web/src/components/seo/SeoChatComposer.vue`、`apps/web/src/components/layout/AppShell.vue`、`apps/web/src/components/common/AppMessage.vue`、`apps/web/src/components/agent/*`、`apps/web/src/hooks/useSeoWorkspace.ts`、`apps/api/src/seo/prompts/seo-agent.prompt.ts`、`docs/work-log.md` | `pnpm --filter @agent/web lint`、`pnpm --filter @agent/web typecheck`、`pnpm --filter @agent/web build` 已在改动过程中通过；提交前 `pnpm typecheck`、`pnpm lint`、`pnpm --filter @agent/web build` 通过 | 继续 T19：在后端 chat 请求中加入受控 history，同时保持前端组件库只服务复杂交互 |

## 记录规则

- 每次阶段性开发、重要讨论或 commit 前后补一条记录。
- 如果记录发生在 commit 前，`提交` 写 `待提交`；如果已经 commit，写短 hash 和提交信息。
- 只记录核心事实：目标、完成内容、业务进度、关键决策、验证结果和下一步。
- 项目完成路径和业务任务状态维护在 `docs/development-task-plan.md`。
- 学习概念写入 `docs/learning-log.md`；项目推进和 commit 上下文写入本文件。
- 不记录 API Key、token、密码、私有服务密钥等敏感信息。
