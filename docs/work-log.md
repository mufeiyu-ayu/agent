# 项目工作记录

本文件只记录近期关键里程碑。旧阶段细节查看对应 Task 文档、Issue / PR 和 Git 历史。

## 当前快照

当前状态只在 [`tasks/README.md`](./tasks/README.md) 维护，本文件只记已发生的事实。

## 近期关键记录

| 日期 | 事项 | 结果 |
| --- | --- | --- |
| 2026-09-23 | docs 复核纠错 | docs-only，直接提交 master。看板：修 #155 行里被当成列分隔符的竖线（根 lint 新增失败），#137 行改为已接受，#144 行注明 80ms 批处理已被 `c9c49ef` / #155 取代，#151–#158 各行补复核确认的限制，规则允许小改动例外；README 中英文改掉「每个改动都从 Issue 开始」、「exact request body」、「local tokenizer 计数」、「引用能被证明」等过度表述，统计改为约数，Next 与工作台第 7 节对齐；roadmap / admin-console / phase-08 设计稿的状态快照改指针，roadmap 补 src / dist 保留决定与 #146 合并日期；根导图补请求参数不落库与 U+0000 只覆盖 jsonb，admin / ai 导图补测试入口与 `detail` 实况；pi-reference roadmap 注明 #152 已按字段落库、鉴权改在第 3 档；workflow 写明验收后只允许 docs 收口提交、合并后才到的云端评论在下一个同模块 Issue 立项时分拣。复核确认 `f0b8d57` 改的是凭据路径（前台余额解密另一服务商密钥并回退默认服务商），不属于小改动例外，其回退已由 #156 删除；4 个直推 commit 都没有 review 记录 |
| 2026-09-23 | 家里电脑主库容器按 compose 重建 | `docker compose up -d postgres`：`agent-ai-seo-postgres`（0.0.0.0:5432）重建为 `agent-postgres`（127.0.0.1:5432），卷 `agent_postgres-data` 不变，会话 / 消息 / Run / 服务商 / 模型行数前后一致（14 / 32 / 16 / 4 / 13）；测试库同样重建为 `agent-postgres-vector-test`（127.0.0.1:5433） |
| 2026-09-23 | #151–#158 只读复核 | 另一台电脑上 6 个 agent 复核当天 8 个 PR：原审查的 P0 / P1 在代码层均已修对，PR 之间没有互相破坏；typecheck、四个包 lint、`@agent/ai` 67、api 16 组（含 3 组 DB）、admin check、web 81 全绿，e2e 未跑。新发现：模型可见文本带 U+0000 会让 Run 停在 RUNNING（PostgreSQL text 列拒收，`Message.content` 未清洗）、中文标点紧贴 `**` 时终态显示字面标记、流式补齐当前行误补强调、预算校验放行旧坏行、7 条合并后才到的 Codex 评论无人分拣、3 个 Issue 的实现差异未回写；另立 Issue 修复 |
| 2026-09-23 | #158 运行时小修合并 | PR #166（代码 head `3223930`，Closes #158）：20 files，+340 / −88，无 migration。`invalid_arguments` 不再计为 eligible 工具失败、也不建 Grounding Session；跨轮用户可见文本之间保证空行（含 Grounding 重放）；planner 裁剪按问答对齐；`test:tools` 排除 db 测试、`@agent/ai test` 多层匹配、新增根脚本 `test:dev-apps`、`test:llm-config` 先构建 contracts；缓存字段只认有限数；`capabilitiesOf` 用 `Object.hasOwn`；删 `TokenEstimator.strategyId` 与注释漂移。AC-01～AC-06 逐条 PASS |
| 2026-09-23 | #157 真实 Tool Call 流 fixture 合并 | PR #165（代码 head `8d95252`，Closes #157）：16 files，无 migration。新增一次性录制脚本，四份真实 Tool Call SSE（deepseek 直连、中转站 gpt / grok、Google 官方直连 gemini）经真实 client 与 SDK 解析回放；累加器与 debug tee 对重复 id / name 不再拼接；`LlmFamilyCompat` 加 `toolCallIndexOptional` / `toolCallsMayFinishWithStop`，只对 gemini 放宽缺 index 与带调用的 stop。中转站 gemini 当天全线 upstream_error，gemini 改由 `.env` 的 `GEMINI_API_KEY` 直连录制；直连 Gemini 3 续轮缺 thought_signature 会 400，未处理 |
| 2026-09-23 | #156 低成本安全加固合并 | PR #164（代码 head `1f4616d`，Closes #156）：26 files，+586 / −88，无 migration。库内密钥只发往库内地址（拉取 / 测试只带 providerId 用库里 baseUrl，改地址须重填 key，地址与密钥成对写入）；未映射状态码错误文案不带上游 body；余额只查 https 官方 DeepSeek 账号并投影声明字段；API 去 CORS、只监听 127.0.0.1（dev 探测与 Vite 代理同步），compose 主库绑 127.0.0.1，`.env.example` 主密钥留空、补 `PORT`，GCM 标签固定 16 字节，测试 key 换假值。本机主库容器随 `docker compose up -d` 重建为 `agent-postgres`（卷不变）。AC-01～AC-09 逐条 PASS |
| 2026-09-23 | #154 管理台模型接入页与小修合并 | PR #163（代码 head `a2570ba`，Closes #154）：26 files，+638 / −162，无 migration。家族 / 地址 / 密钥变更重载模型表并清探活结论，弹窗测试结果按代次丢弃，中止静默；探活条件写回；预算校验复用 `resolveInitialContextBudget`；探活 / 拉取 / 余额 30s 上界（`@agent/ai` 元数据请求接受 signal），403 文案带状态码，拉取 4xx 给 `/v1` 提示不回显 body；Inspector 回退读 finalization input；复制加 execCommand 回退；删 32 个孤儿 i18n key 并加引用检查；e2e 截图改到 `.artifacts`。AC-01～AC-10 逐条 PASS |
| 2026-09-23 | #155 前台流式渲染与模型自恢复合并 | PR #162（代码 head `6dad027`，Closes #155）：18 files，+778 / −101，只改 `apps/web`。流式补齐按行 / 列表项 / 单元格为边界、setext 下划线在文末先不显示、尾块不走缓存、图片渲染成链接；模型不可用 400 后与打开下拉时重拉模型并纠正选中项，强度默认不发送；delta 按帧合并 + 轮次 v-memo（80 轮 400 delta TaskDuration 1559→1091ms）；i18n localStorage 保护、失败不置顶、删死代码与孤儿 key、e2e 默认端口 5176。AC-01～AC-08 逐条 PASS |
| 2026-09-23 | #152 Run 轨迹补齐模型可见内容合并 | PR #161（代码 head `c75119e`，Closes #152）：28 files，+1,480 / −137，无 migration。tool Step 落回喂参数与 observation，采样 Step 落 Tool Call 轮文本 / reasoning 与历史条数、observation 实际送入长度，finalization 落提示词三个标量；Admin Run Trace 展示并按纯文本渲染；根与 `apps/api` 导图的 model-visible ⟺ logged 改为 action 循环内成立并写明范围。AC-01～AC-07 逐条 PASS |
| 2026-09-23 | #153 管理台概览与运行列表按健康 / 延迟 / 用量 / 工具重构合并 | PR #160（代码 head `4e54a72`，Closes #153）：42 files，+2,161 / −1,090，无 migration。概览接口改 SQL 聚合只取必要 JSON 路径，Token 统一 totalTokens 并计入 finalization attempts；模型调用口径在列表 / 详情 / 概览同源；运行列表加模型 / 失败原因列与 errorCode 筛选；概览不再请求模型目录、余额竞态修复；`@agent/ai` grok outputTokens 归一。AC-01～AC-08 逐条 PASS，dev 库 30d 三方 Token 均为 159,305 |
| 2026-09-23 | #151 模型调用失败保留真实原因合并 | PR #159（代码 head `1706a2d`，Closes #151）：39 files，+1,263 / −127，含 migration `20260923120000_agent_run_error_code`（`AgentRun.errorCode` 可空列）。失败类别与用户文案在终态确立后一处得出，Run / Step / error 事件同源；采样 Step 记 `firstTokenMs`；Admin Run Trace 展示两项。AC-01～AC-07 逐条 PASS，dev 库已 `migrate deploy` |
| 2026-09-23 | 近 7 天改动只读审查 | 6 个 agent 对近 7 天改动做只读审查，基线全绿；主要发现：库内 API Key 可被发往任意 baseUrl（无鉴权 + 局域网暴露）、模型调用失败原因被统一改写为「模型流读取失败」、model-visible ⟺ logged 不成立、概览 Token 口径与整列读 JSON、前台流式标题闪烁与外链图片；拆为 #151–#158（Planned，#151 为 Next），鉴权按第 3 档暂缓 |
| 2026-09-23 | docs 整合：路线单一来源、状态单点、过期导航纠错 | docs-only。顺序与触发只在 `docs/research/workbench-direction.md` 第 7 节，`pi-reference/roadmap.md` 只留每步 Pi 素材与「证明完成」，`workflow.md`「下一步做什么」改按第 7 节与看板提议；当前状态只在 `docs/tasks/README.md`，根 `AGENTS.md`、`docs/README.md`、`roadmap.md` 与本文件的状态快照改指针；看板补学习欠账一行；根与 `apps/api` 导图的 model-visible ⟺ logged 标为 R1 目标、当前不完全成立；`packages/ai` 导图重试描述改为与代码一致、两个包导图写清 exports 的 types → src / import → dist；web 导图补 `test` 与流式渲染入口；`agent-runtime` / `retrieval` README、`configuration-map.md`、`admin-console.md`、`current-agent-mapping.md`、根 `README.md` 过期项修正，Pi 研究文档去掉研究机的 `/Users/ayu/...` 前缀（本仓库文件改相对链接，Pi 源码位置改写为相对 Pi 仓库根的 `packages/…:行号`）；`github-issue-workflow` skill 收口步骤不再要求改 `roadmap.md`；`workflow.md` 加小改动例外与高风险 Issue 自拟故障注入 AC；删 `context/ai-seo-agent-plan.md`（2026-05 SEO 规划，#134 漏删）。同日用户决定写入：鉴权暂缓到「第一个同事要用」（第 3 档），局域网可达不算，只做低成本加固另立 Issue；`web_fetch` 保留 Gated、放第 7 节「后」行；长会话首轮同步分词（1000 条 × 约 2.4k 字符实测 4.6s，dev 库最长 18 条）记后置，触发为真实会话超过约 200 条或首轮 plan 超过 500ms；直接合 master 的小改动是用户惯例 |
| 2026-09-22 | 删 `SamplingContextPlanSummary` 无消费者的观测字段（直接合 master） | `4a35ac4`，无 Issue，小改动惯例：分支改完、review 后合 master。源码阅读核对 contextPlan 落库链路时发现 `toPersistedContextPlan` 只写三个字段，`samplingIndex / toolExchangeCount / estimatorStrategyId` 在生产链路与测试断言中均无读者，与 #149 同类；删这三项与 `plan()` 的 `samplingIndex` 入参（`AgentStep.input.samplingIndex` 保留），history 计数与 observations 因测试仍断言而保留。3 files，+1 / −27；api typecheck、`test:context` 28、`test:model-stream` 82、`test:admin` 65 通过 |
| 2026-09-22 | 前台余额接口改查自有 DeepSeek 官方账号（直接合 master） | `f0b8d57`，无 Issue，小改动惯例：分支改完、review 后合 master。`/api/llm/balance` 与管理台概览同用 `resolveBalanceProvider`：优先启用的 `api.deepseek.com` Provider，没有或密钥解不开时退回默认模型所属 Provider；默认模型挂在中转站下时前台余额不再恒为不可用。1 file，+3 / −3；api typecheck / lint、`test:llm-config` 7 项通过，本机接口实测返回自有账号余额 |
| 2026-09-22 | 管理台概览页接真实数据并补后端窗口指标合并 | PR #148（无 Issue，规格以 PR 正文为准；merge `4ac0ebe`）：19 files，+1,677 / −546。前端删除 mock 仪表盘的 9 个组件与 `mock-data.ts`，概览页四块（运行六格、Token / 运行频次折线、工具调用、模型用量与健康表）接 `/api/admin/overview/stats`、余额与模型配置，余额收成顶栏一格；后端 `stats` 加 `window=24h/7d/30d`（24h 精确最近 24 小时按整点 25 桶），`AdminOverviewStats` 改为 `window / bucket / points`，新增缓存命中 / 输入 Token 汇总与每模型平均时长（只算 COMPLETED），余额优先自有 DeepSeek 官方账号；一轮 `/code-review high` 9 条 finding 全部处理。前台 `/api/llm/balance` 语义未动 |
| 2026-09-22 | 前台流式渲染改按块记忆化与平滑放出（直接提交 master） | `c9c49ef`：用户授权跳过 Issue。Markdown 按顶层块拆分并按块源码复用 HTML，尾块补齐未闭合标记，渲染层按帧平滑放出、结束后快速收尾，提交间隔按解析耗时自适应；一轮结束不再重拉 `/api/conversations`，改本地置顶；grounding e2e 截图改落 `.artifacts`。两轮 `/code-review high` 共 19 条 finding，修 15 条；web 单测 72 项、chat-ui 与 grounding 20 项 Playwright 通过 |
| 2026-09-22 | #146 中转站各家族协议差异收口合并 | PR #147（head `0b5894c`，Closes #146）：29 files，+1,094 / −84。`LLM_FAMILY_CAPABILITIES` 扩成 compat 表（`thinkingFormat` / `requiresReasoningContent` / `reasoningEfforts`），`LLMModelProfile` / `ResolvedChatRequestConfig` 用 `compat` 替换 `reasoning: boolean`，删 `isThinkingFamily`；`toModelUsage` 缓存命中按 `prompt_tokens_details.cached_tokens ?? prompt_cache_hit_tokens ?? cached_tokens` 兜底、只有命中数时推导未命中数，`inputTokens` 口径不变；`packages/ai/src/api/__fixtures__/` 新增 deepseek 直连 / 经中转、grok、gpt、gemini 的真实聚合响应（`scripts/export-raw-response-fixtures.ts` 只读导出，不含 `reasoning_content`）与手工 chunk 序列，`openai-completions-fixtures.test.ts` 期望值写死；web e2e 模型桩对齐 `ChatModelOption[]`，`reasoning-effort.spec.ts` 恢复。两轮 `/code-review high` 共 16 条 finding，修 13 条。AC-01～AC-06 逐条 PASS：本机 grok / 经中转 DeepSeek 第二轮 `promptCacheHitTokens` 2304 / 1280 与原始 `cached_tokens` 一致；fixture 字段名改动故障注入 51 → 50 通过。学习环节待做 |
| 2026-09-22 | #149 删 #119 后无读者的初始上下文观测字段与 Admin Context Inspector 来源分区合并 | PR #150（head `a7fbe33`，Closes #149，merge `55c56f2`）：12 files，+45 / −394。源码阅读走到 `summarizeInitialContext` 时发现 #119 合并裁剪后遗留的观测字段无读者，用户决定五行端到端删、「Context 结果」保留。contracts 删 `AdminContextInspector` 的 history 三项与 `observations`、`AdminModelSamplingStep.providerItemCount`、`AdminContextObservationSummary`；runtime `InitialContextSummary` 只剩 `resolvedModel / providerId / modelId / resolvedInputBudgetTokens`，`load_conversation_history.output` 只写 `messageCount`，采样 Step 不再写 `messageCount`，`contextPlan` 快照只写预算 / 估算 / `overflowReason`；admin 删「来源」分区与 Observation #N，e2e fixture 对齐 contract。planner 内部 `summary` 的计数与 observations 保留（算法返回值，仅不再落库）。一轮 `/code-review high` 2 条 finding 全修。AC-01～AC-05 逐条 PASS：全仓 typecheck、`test:context` 28 / `test:model-stream` 82 / `test:admin` 65、admin 三个 check；legacy fixture 投影不泄漏。源码阅读中的顺手改动，无学习环节 |
| 2026-09-21 | 修复局域网 HTTP 下无法发送消息（直接合 master） | `5e194c0`，无 Issue，小改动惯例：分支改完、review 后合 master。局域网 HTTP 不是安全上下文，没有 `crypto.randomUUID`，发送前抛错；`useChatWorkspace.ts` 改用 `getRandomValues` 生成临时 ID，`e2e/chat-ui.spec.ts` 补缺少 `randomUUID` 时的发送回归。2 files，+24 / −1；类型检查、定向 lint、两项浏览器回归通过 |
| 2026-09-21 | 管理台模型表 Token 列精简（直接合 master） | `6bbd9c6`，无 Issue，小改动惯例：分支改完、review 后合 master。`LlmModelTable.vue` 的 Token 列去掉「上下文 / 输出」小字标签只留数值（Tooltip 仍给全称），删对应两组 i18n key。2 files，+2 / −23；commit 未记录验证 |
| 2026-09-21 | #144 前台对话 UI 审查修复与验收 | PR #145，代码 head `c8f903d`，AC-01～AC-06 逐条 PASS。保留代码卡片、阅读定位与 Tooltip，修复 Markdown 语义 / 空白、未闭合终态、暗色 CSS、滚动状态、恢复竞态与复制失败；去掉流式前缀缓存、重复调度和未使用配置。独立 reviewer 的中止/失败重建卡片、返回生成会话不跟随两条均浏览器复现后修复。web typecheck / lint / build、54 单测、10 Chromium 回归通过；未做 CPU 基准、真实 Provider 或人工视觉验收，不采信“CPU 降低80%”说法。学习环节待做 |
| 2026-09-20 | #142 模型配置入库合并 | PR #143（head `87cefcc`，merge `39edad1`，Closes #142）：5 个功能 commit。配置入库（`LlmProvider` / `LlmModel`、AES-256-GCM 密钥、三条 migration）、删 `LLM_*` env；本机冒烟后收口：导入带弹窗测试状态、删 `reasoning` 开关改按家族固定、模型行 `reasoningEffort`（`LLM_FAMILY_CAPABILITIES` 一张表，直打中转站实验：gpt-5.6-sol 推理 token low 153 / high 233、grok-4.6 low 4221 / high 8676，Gemini 无法验证暂不开放）、导入预设按家族官方上限、管理台交互（图钉默认 / 状态即重测 / 强度直选 / 「模型接入」）。review：bug 9 条全修（probe 对齐真实 Run 参数、改家族清强度、设默认单条 SQL、余额只对 deepseek、收回主密钥导出）；可维护性 15 条改 8 条（拉取 / 测试合成两条路由、删非流式 `chat()`、删 `LlmIcon`、家族三表合一、state 去接口抽 `loadList`），净删约 430 行；错误文案去厂商名。验收 AC-01～AC-11 PASS 记于 PR 评论。学习环节待做 |
| 2026-09-20 | 给模型的路径导图落地 | 三个 app 与两个包各加一份根目录 `AGENTS.md` 导图（同目录 `CLAUDE.md` 为一行 `@AGENTS.md` 导入壳），只写入口、分层、核心文件与不变量，单份 30–45 行；根 `AGENTS.md` 第 4 节改为导图索引、第 11 节加同步行；`docs/workflow.md` 硬约束新增：改某 app / 包前先读其导图，commit 前若新增 / 移动 / 删除了导图提到的模块或核心文件则同一提交同步，导图由模型自己维护 |
| 2026-09-20 | 产品方向定案：内部数据工作台 | 讨论会话（09-19 至 09-20）定案：合并公司 gsc 数据观测项目（Python CLI + Next 看板，GSC / GA4）为一个内部系统，工作台 `apps/web` 放固定页面与 agent 对话（容器写代码、右侧 iframe 预览、下载），管理台 `apps/admin` 放接入 / 同步 / 配额 / 权限 / 可观测；数据层先迁入 API、页面按常用度移植、Python CLI 退役；凭据不进容器、不自由写 SQL、走缓存与配额层；OS sandbox 进第 1 档，web_fetch 空位换成 gsc 数据工具，分包仍在 R2；三档推进与触发、否决项与未决记入 `docs/research/workbench-direction.md`；路线正文待源码阅读完成后改写。同日核实 DeepSeek 文档：Chat / Responses 两端均无托管 web_search |
| 2026-09-19 | #137 Gemini embedding 重试交给 SDK 合并 | PR #141（`79b2179` + docs 收口，Closes #137）：6 files，+199 / −444。`gemini-embedding.provider.ts` 424 → 301 行：删手写重试环（`embedBatch` 循环、`sleepWithAbort`、`readRateLimitRetryDelayMs`、`withMetrics`、四个退避常量、`retrySleep` 注入、`EmbeddingError.retryAfterMs`），`retryOptions` 由 `{ attempts: 1 }` 改 `{ attempts: 3 }`；SDK 耗尽后可重试类错误映射为 `retry_exhausted`，caller 分支不变；删 `EMBEDDING_BATCH_SIZE / EMBEDDING_REQUEST_TIMEOUT_MS / EMBEDDING_MAX_RETRIES` 与 `readInteger / invalidConfig`，改为 `embedding-provider.ts` 常量 64 / 60_000 / 2，`EmbeddingRuntimeConfig` 收为 `{ apiKey, model, dimensions }`。读 SDK 源码确认并记录三处行为差异：`ApiClient` 用 p-retry@4 纯指数退避、不读 `Retry-After`；Node `TypeError: fetch failed` 不被 SDK 重试，Provider 直接按 `network` 上报（原有 2 次重试，是否接受待用户拍板）；abort 落在 SDK 退避期间延迟 ≤ 4s（Run 路径已 race）。验证：api typecheck / lint，`test:article-indexing` 60、`test:retrieval` 34、`test:tools` 80，AC-01 grep 0，`index:articles` 单篇 full 对真实 Gemini 成功。`/code-review high` 3 条：修 1 条注释，2 条按 Issue 范围不修并记录 |
| 2026-09-19 | #136 零行为变化小修合并 | PR #140（`03a5713` + docs 收口，Closes #136）：零运行时行为变化，49 files，+663 / −1,486。tools：删 `ToolDefinition.risk / requiresApproval / idempotent` 与 `ToolInvocationService` 17 行准入门（对已注册工具永远不触发；R3 审批门按运行时状态重加），删 `tool.errors.ts` / `TOOL_NAME_PATTERN` / `model-tool-spec.mapper.ts`，`ValidatedToolInvocation` 只剩 `toolName / input`、`ToolExecutionContext` 只剩 `databaseDeadline / signal`、`UnvalidatedToolCallEnvelope` 删 `samplingAttemptId`（runtime tool Step input 改用循环内同值变量）、`ToolResult` 删 `data` 与泛型，三份工具清单合成 `tools/tool-definitions.ts` 的 `TOOL_DEFINITIONS`（Run allowlist 与 Admin evidence policy 共用；`ToolsModule` 三次 register 因 definition / executor 泛型成对保留）。agent-runtime：`InitialContextSummary` 14 → 6 字段、快照只估算必带内容一次（planner 测试估算上界 2+3+⌈log2 n⌉ → 1+3+⌈log2 n⌉），service 直接 import recorder 的 `CloseAgentStepInput / CloseAgentStepMetadata`，`fromFinishReason` 改查表并收窄参数，四个事件 interface 内联进联合体，内联 `toLlmRole / isAbortSignalTriggered`，module 只 export `AgentRuntimeService`。admin / contracts：七个 allowlist 改 contracts `as const` 数组派生 union（`ADMIN_MODEL_FINISH_REASONS` 等 5 个 + `MESSAGE_EVIDENCE_AVAILABILITIES` / `MESSAGE_GROUNDING_OUTCOMES`，`parseMessageGroundingV1` 同源），删 `AssertAssignable` 守卫与 `AdminRetrievalContractGuards`，导出 `AdminRunKnownTimelineItemBase`，五个镜像接口改 `Prisma.AgentRunGetPayload`（select 从 service 导出、projector `import type`），`context-inspector.projector.ts` 并回，夹具抽 `projection/__fixtures__.ts`，删 `readPositiveInteger` 与 contracts `AgentRun / AgentStep / AgentStepJsonValue / ApiResponse`，`admin-overview` 复用 `readString(o, k, Infinity)`（空串 `\|\| null` 保持语义），`createAppValidationPipe()` 无参。与 Issue 差异：AC-01 `samplingAttemptId` 按 tools 目录解读；「四个 reader 合一」需新增谓词抽象故不做；DTO 测试用 `createAppValidationPipe()`。验证：typecheck 6/6，api / admin / contracts lint，`test:tools` 80、`test:tool-loop` 5、`test:model-stream` 83、`test:context` 29、`test:grounding` 154、`test:chat-service` 28、`test:llm-config` 2、`test:agent-recorder` 21、`test:retrieval` 34、`test:admin` 63，admin 三个 check；本机 dev 真实对话 Run `cmu8b339y0002wigwh79o8bs6`（16 Step、grounded finalization `unavailable / insufficient_evidence`）+ Admin Run Trace / Retrieval / Context Inspector 零 console 错误；`retrieve_article_context` 30s timeout 为 dev API 进程无代理环境所致。`/code-review high` 2 条 Low 已修。可见变化：无 |
| 2026-09-19 | #135 删离线评估 baseline 与无运行记录 smoke 合并 | PR #139（`eb767ea` + docs 收口，Closes #135）：零行为变化，19 files，+73 / −1,811。删 v1 检索 baseline 闭环（`retrieval-baseline.ts` / `.cli.ts`、`retrieval-evaluation.ts` / `.test.ts`）与 `eval:retrieval-baseline`，`validateRetrievalResult` 原样搬入 `article-retrieval-quality-v2.ts` 作内部函数，`ArticleRetriever` 去泛型默认值；删 `grounded-answer.smoke.cli.ts` / `embedding-smoke.cli.ts` 及测试与 `smoke:grounded-answer` / `smoke:embedding`；`retrieve-article-context.smoke.cli.ts` 保留、去接口与三个 export，默认查询词改「Wuthering Waves 的 soft pity 是什么」；删空目录 `insight/`、`dev:watch` / `predev:watch` / 根 `dev:api:watch`，三条 `test:admin-*` 合成 `test:admin`，`agent-runtime.service.test.ts` 只归 `test:model-stream`；`.env.example` 删 `GROUNDED_ANSWER_SMOKE_*`；docs 旧路径 5 处加标注。验证：typecheck 6/6、api lint，`test:retrieval` 34、`test:article-indexing` 60、`test:grounding` 154、`test:tools` 86、`test:admin` 63、`test:model-stream` 83、`test:tool-loop` 5 全绿，无 DB 环境两个 CLI 进入 main。`/code-review high` 无 finding。学习环节待带读 |
| 2026-09-19 | #134 去 SEO 产品命名合并 | PR #138（`c249f7c` + docs 收口，Closes #134）：后端 `seo/` → `chat/`（`ChatModule` / `ChatController` `@Controller('chat')`，路由 `/api/chat/stream`），删非流式 `chat()` 链路、`SeoChatResponse` 与只为它导入的 Nest 异常，`buildSeoAgentInstructions([])` 改常量 `AGENT_INSTRUCTIONS`，手写 `StreamResponse` 改用 `node:http` `ServerResponse`（`@types/express` 未安装，不为类型加依赖）；系统提示词改通用助手、保留贾维斯、工具段改「站内文章知识库」；env `SEO_CHAT_HISTORY_CANDIDATE_HARD_LIMIT` → `AGENT_HISTORY_CANDIDATE_HARD_LIMIT`；contracts `seo.ts` → `chat.ts`（`CHAT_MESSAGE_MAX_CHARS` / `ChatRequest`，删 `ChatStreamProtocol` / `ChatStreamEventType` / `SeoChatResponse`）；前端 `components/seo/` 拆为 `home/` + `chat/`，`useChatWorkspace` / `useFlow*` / `api/chat.ts` / `ChatWorkspaceView`，`Seo*` 去前缀，`data-*` 与流程节点 id 按职责命名，i18n 与流程图 / 简报文案改为知识库问答口径，`index.html` 标题「Agent 工作台」；根 package name `agent`，`.env.example` / `docker-compose.yml` 示例库名 `agent` / `agent_integration`（既有卷与真实库不动，healthcheck 去掉 `-d`）；README / AGENTS / research 口径同步。CSS class、`<style>`、SVG defs id 按 AC-02 零变化保留 `seo-*`；测试 fixture、smoke / baseline SEO 数据不改（后者归 #135）。67 files，+759 / −944（含讨论会话未提交的 docs 状态同步 `02bbcc8`）。验证（基于 head `c249f7c`）：typecheck 5/5，api / web lint，`test:chat-service` 28、`test:tool-loop` 70、`test:model-stream` 83、`test:grounding` 174、`test:context` 29、`test:tools` 86、`test:retrieval` 42、`test:llm-config` 2、`test:admin-runs` 55、web test 44、`@agent/ai` 48、web build，`git diff --check` 通过；本机 dev 真实对话 `/api/chat/stream` start → delta×41 → done，Admin Run Trace 正常，页面文本与 aria 不含 SEO。`/code-review high` 1 条 Low（旧卷 + 新 `.env.example` 库名不一致）按 README 说明修复。可见变化：路由 `/api/seo/chat/stream` 404、`/api/chat/stream` 生效；模型不再自称 SEO 顾问；工作台主题偏好因 localStorage key 改名重置一次；`docker compose up` 下次会按新容器名重建（数据在具名卷保留）。学习环节：待带读与独立改一处 |
| 2026-09-19 | 过度设计审计与四件立项、web_fetch 转 Gated | 讨论会话六路并行审计 `apps/api/src`、`packages/ai`、`packages/contracts`（方向：过度设计残留、分包边界、重复、死代码、测试比例、规模热点），每条 finding 附 grep 消费者证据；结论：`packages/ai` 零 Nest 零 Prisma，`agent-runtime.service.ts` 17 个私有方法无第二消费者不拆，agent-runtime 六个文件与 grounding 六个规则文件已零 Nest 零 Prisma 可进 `packages/agent`；可删约 3,000 行，集中在离线评估与 smoke。立 #134（去 SEO 产品命名，用户定案 UI 样式不变、文本必变、数据层不动）、#135（删 baseline / smoke / 脚本与 docs 死链）、#136（tools / agent-runtime / admin 零行为变化小修）、#137（embedding 重试交 SDK、env 改常量）。web_fetch 转 Gated：用户确认暂无反复读网页的真实用途。DeepSeek 文档核实：Chat / Responses 两端均无托管 web_search（Responses 页标 Ignored），web_search 仍需第三方 |
| 2026-09-19 | #116 验收补修合并 | PR #133（`cdcb0ab` + docs 收口，Refs #116）：PR #132 Codex 行内 P2 与外部验收审查指出，未经工具输入契约校验的原始参数（`unknown_tool` / `invalid_arguments` 为 #116 前已存在路径，`truncated_arguments` 为 #116 新增）回填后，生产 `DeepSeekV4TokenEstimator` 对 `[]` / `null` / `1` / `{"0":"x"}` / `{"nested":[1.5]}` / `{"x":1e999}` 等形态抛 `ContextTokenEstimationError`，下一轮 Run FAILED，违反 AC-06；PR #132 的 AC-06 回归只用了 `TestTokenEstimator`。修复：`ModelContext.appendToolExchange` 的 `results[]` 新增必填 `argumentsValidated`，已校验参数原样续传（两处 `parse` 拒绝非对象与多余键、数值只收整数，已校验 ⇒ 在 estimator 已验证子集内，固定向量不变），未校验参数续轮表示改为 `{"arguments": raw}`（DeepSeek 官方编码器对不可解析参数的回退形状；原文保留在值里、wire 为合法 JSON、Planner 估算与实际请求同一份表示）；Runtime 按 result code 判定；estimator 守卫不删、`ToolResult` 契约不动。回归：原始 provider chunk → 真实 adapter → 真实 Runtime → ModelContext → 真实 Planner → 生产 estimator → 下一轮请求，`length` 路径 13 种 raw arguments 与 `invalid_arguments` / `unknown_tool` 各 2 种，并断言每轮实际请求 deepEqual estimator 最后一次估算输入；`packages/ai` 真实 SDK 序列化 + fake fetch 断言 wire body 的 `function.arguments` 原字符串；修复前 raw=`[]` 以「DeepSeek V4 TokenEstimator 无法完成请求前估算。」FAILED，修复后通过。6 files，+300 / −10。验证（验收基于 head `cdcb0ab` 复跑）：typecheck 6/6，`@agent/ai` 48/48，`test:tool-loop` 70、`test:model-stream` 83、`test:context` 29、`test:grounding` 174、`test:tools` 86、`test:admin-runs` 55，lint 与 `git diff --check` 通过；真实 Provider 冒烟未执行。`/code-review high` 1 条 Low（policy 拒绝的 `execution_failed` 边界，当前不可达）按范围不改。可见变化：模型发出非对象 / 超出子集的参数、或参数被 `length` 截断后，下一轮不再因估算失败而 Run FAILED；失败调用在续轮历史里以 `{"arguments": raw}` 形状出现。按本次授权流程：Draft PR → 独立验收 → 用户授权收口后转 Ready 合并 |
| 2026-09-19 | #116 同轮文本 + 多 Tool Call 合并 | PR #132（`2d62cfd` + docs 收口）：sampling decision 重写为只按 `finishReason` 分派，`SamplingDecision.tool_call.calls[]` 与 `ModelInputItem.assistant_tool_call.calls[]` 承载同轮多 call，Chat 映射为一条带 `tool_calls[]` 的 assistant 消息；runtime 顺序执行、每 call 一个 `tool_execution` Step、`maxToolCalls` 按 call 计数并在执行前整批拒绝；`finishReason === 'length'` 时整批不执行、逐 call 记 `truncated_arguments` 回喂、Run 继续；Grounding 模式带 Tool Call 轮次的文本只回填模型，非 Grounding 已推出的 delta 保留（`Message.content` = 中间文本 + 最终回答）；adapter `finalize(truncated)` 在 `length` 时放行有 id 与 name 的分片、丢弃无法配对分片、同批重复 call id 抛 `LLMApiError`，第四条不变量加 `length` 例外，reasoning_content 不变量覆盖所有会回填的 Tool Call；policy 删 `maxToolCalls < maxSamplingRounds`；删 `ChatMessage` / `toModelInputItems`，`ModelInputItem` 三分支具名导出；finalizer 删 `extra_event_after_completion` / `missing_submission`，contract 同步并加反向漂移守卫；`AdminToolResultCode` 加 `truncated_arguments`；DeepSeek estimator 多 call 渲染进同一 tool_calls 块、裸 NaN / Infinity 守卫忽略字符串字面量。34 files，+1,276 / −763。验证（验收基于 head `2d62cfd` 复跑）：workspace typecheck 6/6，api / ai / contracts / admin lint，`@agent/ai` test 47/47，`test:tool-loop` 68/68、`test:model-stream` 81/81、`test:grounding` 174/174、`test:context` 29/29、`test:tools` 86/86、`test:seo-service` 32/32、`test:llm-config` 2/2、`test:admin-runs` 55/55，`grep ChatMessage apps/api/src` 为 0，`git diff --check` 通过。`/code-review high` 两轮 12 条：修 8 条（finalizer 去非空断言、adapter reasoning 不变量覆盖 length、observation 加 `resultIndex`、删 fixture 死字段、contract 删死值加反向守卫、estimator 裸 NaN / Infinity 守卫忽略字符串字面量、截断路径显式 `!argumentsTruncated`、`assistant_output` 顺序注释）；Session 同轮建立前已推出的文本、截断 raw arguments 原样回填、contract 删两个死值按 Issue 决策与 #126 原则不改。可见变化：DeepSeek thinking 先 content 后 tool_calls 或一次多个 tool_calls 不再 Run FAILED；`AGENT_MAX_TOOL_CALLS >= AGENT_MAX_SAMPLING_ROUNDS` 可启动；`.env.example` 删「必须小于」与「同轮单个 Tool Call」注释。剩余风险：截断 raw arguments 原样回填未做真实 Provider 冒烟。学习环节：待带读与独立改一处 |
| 2026-09-18 | #117 关闭转 Gated | 讨论会话核实：Pi（快照 `8a7b0c0`）的 DeepSeek provider 绑 `openAICompletionsApi()`，20 个 provider 走 completions、`openai-responses` 仅 openai / xai；DeepSeek 的 Responses 与 Chat 无能力差异且无状态。#117 两个目的均不成立（接新协议无收益；为证明 LLM 边界写第二实现与「一个实现的接口不抽」相反），以 not planned 关闭并留触发条件评论。顺序改为 #116 → `web_fetch` → R2；看板 / roadmap / README / AGENTS / pi-reference 三份同步 |
| 2026-09-18 | #115 模型调用重试与 Loop 默认上限合并 | PR #131（`4efc5b2` + docs 收口）：`packages/ai` `createClient()` 的 `maxRetries` 由 0 改为常量 `REQUEST_MAX_RETRIES = 2`，交给 openai SDK 6.45.0 内置重试（408 / 409 / 429 / 5xx、连接错误；退避与 `retry-after` 由 SDK 处理），边界为首个响应头之前、流正文中断不重试、不加 env；review 后补 `chatStream` 内 `rejectOnAbort`（SDK 退避 sleep 不监听 signal 且 `retry-after` 无上限，abort / deadline 落在 sleep 期间也立即抛 `LLMNetworkError`）与 `AbortSignal.any` 派生一次性 signal（SDK 每次尝试在 signal 上挂监听不移除，10 轮 + 1 次重试即触发 `MaxListenersExceededWarning`）；policy 默认 `maxSamplingRounds` 3 → 10、`maxToolCalls` 2 → 8，`.env.example` / README 同步并注明两个上限要一起覆盖；timeout 注释改为「只约束到首个响应头、每次尝试各自计时」。`openai-completions.test.ts` 新增 8 个经真实 SDK + fake fetch 的用例，三处变异检查各自击中对应用例。7 files，+329 / −29。验证（验收基于 head `4efc5b2` 复跑）：`@agent/ai` typecheck / test 43/43，api typecheck / lint，`test:tool-loop` 65/65、`test:tools` 86/86、`test:llm-config` 2/2、`test:model-stream` 78/78、`test:admin-runs` 55/55、`test:grounding` 172/172，`git diff --check` 通过。Review：`/code-review high`（Opus fork）3 条 + Fable agent 3 条同时命中 abort 被退避 sleep 拖延；Fable 复审 2 条（监听累积、旧 timeout 注释不成立）；修 5 条，非流式 `chat()` / metadata 请求无 signal 的最坏耗时（≈ 3 × timeout + 退避）按范围记录不改。可见变化：单次 429 / 5xx / 连接错误不再让 Run FAILED；检索两次再作答不再触发 `AgentLoopLimitExceededError`；只 pin 了 `AGENT_MAX_SAMPLING_ROUNDS ≤ 8` 的 `.env` 升级后启动失败（本机未设置）。学习环节：待带读与独立改一处 |
| 2026-09-18 | #115 规格修正与 web_fetch 提前 | 讨论会话核对 #115 / #116 / #117 引用的代码事实：只有 #115 范围第 1 条与 #127 冲突，Issue 正文改为 `createClient()` 常量 `maxRetries: 2`、不加 env（AC-01 与决策记录同步）；#116 / #117 只有文件路径过期，开工时刷新，规格不变。顺序定案：#115 → #116 → `web_fetch` → #117，`web_fetch` 不依赖第二 wire 故提前；看板 / roadmap / pi-reference roadmap 同步。预测题一轮（流正文中断不重试、abort 在退避中不再发请求）已过 |
| 2026-09-18 | #126 Review 遗留补修合并 | PR #130（`73c9652`，Refs #126，不重开 Issue）：逐条核实 PR #128 合并时 Codex 云端 Review 留下的 4 条 P2，只修确认仍存在的 F-01 / F-02。F-01：projector `readSourceRefs` 改用 `readChunkIdentity`，`chunkId` 按 `grounding.ts` 口径原样保留（非空、≤ 200 code point），空串 / 超长 / 非字符串整条跳过，不再经 `toPreview` 折叠空白或截断成另一个能与 Citation 关联的身份。F-02：presenter `evidenceRefCount` 改 `number \| null`，任一 call 引用未完整记录（无 summary 或 `refs.length !== sourceCount`）即为 null，明确失败的调用计 0 且其 refs 不计入，`RetrievalInspector.vue` 走 `show()` 显示「未记录」。F-03（RUNNING 期间 finalization 读不到 input 快照）与 S1（概览引用数改用 `registryRefCount`）与 Issue #126 规格一致，记为待决策不改；F-04 停写 `registryTruncated` 属 Issue 要求，不采纳。7 files，+404 / −12，生产改动 3 文件，其余为回归测试。验证（验收基于 head `73c9652` 复跑）：contracts / ai build，api typecheck / lint，`test:admin-runs` 55/55、`test:tool-loop` 65/65、`test:grounding` 172/172，admin typecheck / lint / 三个 check，e2e `retrieval-inspector.spec.ts` 10/10，`git diff --check` 通过，PASS 后转 Ready 合并（`3a488bc`）。`/code-review high` 两轮 11 条：修 5 条、待决策 1 条、按范围不修 5 条。可见变化：Retrieval Inspector 概览「证据引用身份数」在 `get_article_detail` 命中等无 summary 场景显示「未记录」而不是 0 |
| 2026-09-18 | #127 `packages/ai` 运行时配置收敛合并 | PR #129（`95e74c6` + docs 收口）：`config.ts` 186 → 99 行，`LLMRuntimeConfig` 收为 `{ apiKey, baseUrl, model, captureModelIO }`，删 `DEFAULT_LLM_RUNTIME_CONFIG`、`readPositiveInteger`、`MAX_TIMER_TIMEOUT_MS` 与应用硬上限校验；三个 timeout 改 `openai-completions.ts` 常量（10_000 / 60_000 / 600_000）、`max_tokens` 默认 65_536 改 `config.ts` 常量，请求参数不变；`.env.example` 删 `LLM_CHAT_REQUEST_TIMEOUT_MS` / `LLM_STREAM_TIMEOUT_MS` / `LLM_DEFAULT_MAX_OUTPUT_TOKENS` / `LLM_APPLICATION_MAX_OUTPUT_TOKENS`，整数变量注释改为与 `resolveInteger` 实际行为一致；`config.test.ts` 11 → 9 case，`llm.module.test.ts` 改断言 `captureModelIO` 默认 false 与 `LLM_MODEL` 不支持时初始化失败。7 files，+75 / −216。验证：`@agent/ai` typecheck / test 35/35 / build，api typecheck / lint，`test:llm-config` 2/2、`test:model-stream` 78/78，eslint 与 `git diff --check` 通过。`/code-review high` 两轮 7 条：修 5 条，第二轮收回第一轮加的默认 `max_tokens` clamp（超出 Issue 范围、当前 profile 不可达）；残留 env 从校验生效变为静默忽略、`chat` 60s 与 65k 组合按 Issue 范围不改。可见变化：无；已配置这 4 个 `LLM_*` 的 `.env` 不再报错也不再生效。学习环节：Issue 只豁免讨论阶段复述，带读收尾与独立改一处待做（2026-09-23 核对 Issue 后更正） |
| 2026-09-18 | #126 Admin Run 读模型去过度设计合并 | PR #128（`a7a8cf6` + docs 收口）：`packages/contracts/src/admin-run.ts` 458 → 279 行，删 `AdminReceiveUserMessageStep` / `AdminRunSafeStepProjection` / `AdminRunSafeRawData` / `AdminGroundedFinalizationSummary` 与四个 availability / validation / correlation 类型，列表项三个 token 字段合并为 `usage`；`apps/api/src/admin-runs/projection/` 3,163 → 925 行，删全部 `isValid*`、跨字段等式、跨 Step 序列、attempt 状态机复核与 `ADMIN_RETRIEVAL_MAX_*`，改为逐字段「能读就读、读不出就 null」，Generic 只用于未知 `type`，证据工具按 `definition.name` 查 `evidencePolicy`；runtime 同步停写被删字段（sampling / tool / finalization 三种 Step 的 input / output 收窄，删 `ToolResult.retryable`、`ModelContext.snapshot()`、`AGENT_STEP_TYPES.receiveUserMessage`），零行为变化；Admin 只删展示项，`RetrievalInspector` 用 `stepId` 关联 timeline，`messages.ts` 删 62 组孤立 key。47 files，+1,386 / −6,997。验证：typecheck 6/6，api / admin lint，api 7 组测试（admin-runs 51、overview 4、tool-loop 65、context 28、grounding 171、tools 86、recorder 21）与 admin 三个 check 全绿；旧库 Run 投影全部 known、`receive_user_message` 为 generic；本机 dev 服务真实对话落库字段核对通过，Playwright 打开 Run Trace 接口 200、零 console 错误。`/code-review high` 两轮 11 条：修 10 条，`grounding: true` 整行读取按 AC-01 保留。可见变化：Admin 列表无模型列，Run Trace 无「接收用户消息」项与安全原始数据 tab，Context tab 只剩契约保留字段。学习环节：待带读与独立改一处 |
| 2026-09-17 | `test:retrieval-db` 改串行（直接改 master） | `e15b4a3`，无 Issue；commit 正文记用户当日授权直接改 master，归小改动惯例。两个 DB 集成测试文件被 `node --test` 默认并发执行，各自 schema 的 `CREATE EXTENSION IF NOT EXISTS vector` 竞态撞 `pg_extension_name_index`（23505），4 条用例 cancelledByParent；加 `--test-concurrency=1` 后 9 / 9 通过。1 file，+1 / −1 |
| 2026-09-17 | 支持模型收窄为 deepseek-v4-flash | 用户决定只用 DeepSeek Flash，按授权直接改 master：`SUPPORTED_DEEPSEEK_MODELS` / `DEEPSEEK_MODEL_PROFILES` 删 `deepseek-v4-pro`，前端 `DeepSeekModelId` / `FALLBACK_DEEPSEEK_MODELS` / i18n 描述与 `.env.example` 同步；三处以 pro 作「另一个受支持模型」的测试改用 flash 或删除（`@agent/ai` 37 → 36 用例）。`LLM_MODEL=deepseek-v4-pro` 现在启动即 `LLMConfigError`；tokenizer 来源仍指 `DeepSeek-V4-Pro` HF 仓库（encoding 文件出处，非模型名单）。验证：`@agent/ai test` 36/36、api typecheck 与 tool-loop 66/66、llm-config 4/4、model-stream 79/79、web typecheck、eslint 通过 |
| 2026-09-17 | #124 `packages/ai` 目录整理合并 | PR #125（`5e6134f` + docs 收口）：`packages/ai/src` 按 Pi 分层：`llm.errors` → `errors.ts`，`llm-runtime-config` → `config.ts`，`model-profiles` → `deepseek.ts`（并入四个 DeepSeek REST 响应类型），四个 `*.types.ts` 合并为单一 `types.ts`，`clients/` → `api/` 且按协议命名 `openai-completions*`；全部 `git mv` + import 路径修正，`index.ts` 导出集合不变，零运行时行为变化；另修正 5 处研究文档旧路径。验证：删 dist 后 `@agent/ai` typecheck / build 通过、test 37/37，全 workspace typecheck 6/6，api typecheck / lint 通过。`/code-review medium` 1 条 Low 已修。AC-02 的 ≥ 90% 相似度对 `deepseek.ts`（R073）与范围第 3 条不相容，PR 说明后按范围执行。学习环节按 Issue 决策记录豁免 |
| 2026-09-17 | #120 抽出 `packages/ai` 合并 | PR #123（`5840d98` + docs 收口）：新建 `@agent/ai`（零 Nest、零 Prisma，`exports` types→src / import→dist 同 contracts），`git mv` 迁入 `OpenAICompatibleClient`、`adaptOpenAICompatibleStream`、tool-call 累加、raw capture、`llm.types` / `llm.errors` / `model-stream` / `model-input` / `model-tool-spec` 类型、`model-profiles`、`resolveLLMRuntimeConfig` / `resolveChatRequestConfig` 与 6 个测试；`JsonSchemaProperty` / `JsonObjectSchema` 同迁，消除 `llm → tools` 反向依赖；client 去 `@Injectable` / `@Inject` 改收 `LLMRuntimeConfig` 纯对象，`LLMRuntimeConfigService` 留 api（3 行壳），`LlmModule` 用 `useFactory` 装配；apps/api 24 个文件的相对导入改 `@agent/ai`，`llm/` 只剩四个壳；七个显式 build 脚本加 `pnpm --filter @agent/ai build`，`test:llm-config` / `test:model-stream` 按归属拆分，包内 `test` 先建 contracts；`index.ts` 只导出 api 实际引用的符号。零运行时行为变化。验证：删三处 dist 后 typecheck 6/6、api build 通过，`@agent/ai test` 37/37，api 13 组非数据库测试全绿；根 lint 仅 11 个 `docs/research` 文件为既有基线；本机 dev 服务两次真实对话流式与 Admin 投影一致；`smoke:grounded-answer` 因隔离库 public schema 未跑 migration 无法执行。`/code-review high` 两轮 11 条 finding：7 条修复（包内 test 先建 contracts、死测试改正向断言、index 收窄、5 处研究文档路径、README 措辞与 tsx 脚本依赖说明、index 注释位置），4 条按 Issue 范围不修（四个 tsx 脚本加 build、build 前缀合并、adapter 两个导出为运行时回归所需）。可见变化：无。学习环节按 Issue 决策记录豁免，不记「学习已验证」 |
| 2026-09-17 | #119 历史裁剪合一合并 | PR #122（代码 `deb4a4b` + 测试 `bcf76b0` + docs 收口）：删 `InitialContextSelectionService` 的 keyset 分页与批内前缀二分，历史一次 `findMany({ take: hardLimit })`，全部候选进 `ModelContext.initialHistory`，首轮由 planner `excludeOldestHistory` 裁剪；`initialContext` 取裁剪前值（`excludedReason` 只剩 `candidate_cap`），预算删减只体现在 `contextPlan`；`ModelContext` 不再携带快照；`RunTurnStreamInput.buildModelMessages` 改 `instructions`；删 `historyCandidateBatchSize` 与 `SEO_CHAT_HISTORY_CANDIDATE_BATCH_SIZE`。api typecheck / lint 与 context / tool-loop / model-stream / admin-runs / grounding / seo-service 全绿；真实 tokenizer 差分旧算法 68 = planner 68；本机运行服务两轮真实对话 + Admin 投影冒烟通过。`/code-review high` 两轮 11 条 finding：7 条修复（估算改走 `flattenPlanningState`、内联双键上界、planner 独立故障注入、配置地图指向、估算次数上界护栏、差分不阻塞、看板措辞），重复全量估算与 `buildSeoAgentChatMessages([])` 按 Issue 规格保留。可见变化：Admin 的 `candidateCount / messageCount / historyIncludedCount` 变为一次读到的条数，预算裁剪看 `samplingHistoryExcludedCount`。学习环节按 Issue 决策记录豁免，不记「学习已验证」 |
| 2026-09-17 | #118 删死代码与单实现抽象合并 | PR #121（代码 `48681f3` + docs 收口）：删 `receive_user_message` Step 写入、`abortStep`、`ToolRegistryService.require / listDefinitions`、`ContextBudgetExceededError.stage`、`SeoContextBuilder`、`AgentRunConfigurationService`（逻辑并入 `AgentRuntimeService.resolveRunConfiguration`）、`ToolExecutionContext.executionAttempt`、`ModelContext.forSampling` 与 snapshot 明细项，`TokenEstimator` 改 interface 直接注入 `DeepSeekV4TokenEstimator`；+327 / −761、无新增文件。api typecheck / lint 与 13 组非数据库测试全绿，db-reliability 通过，grounding-db / retrieval-db / article-indexing-db 因本机无 `ARTICLE_INDEX_TEST_DATABASE_URL` 未执行。`/code-review xhigh` 两轮 15 条 finding：7 条修复（配置地图指向、snapshot 零拷贝计数、`transitionStep` 去 ABORTED、policy 单次读取、`model: ''` 用例、itemCount 同源断言、fake 清理），其余为 Issue 范围 1 / 3 的直接后果或测试 nit。可见变化：新 Run 的 Admin Run Trace 无「接收用户消息」项；配置解析抛错时 Run 以 0 Step 的 FAILED 落库。学习环节按 Issue 决策记录豁免，不记「学习已验证」 |
| 2026-09-17 | 第四轮独立审查收口 | GPT 基于 `1f3a7eb` 复审：T3 四条全部 ACCEPT，截断分片丢弃策略接受、不加丢弃计数，结论「规格修订收口，可按 #118 开工」；只到规格层，不含实现验收与合并授权。#116 范围 9 补一句：`length` 后全部 buffer 被丢弃时走现有「length 且无 Tool Call」失败路径，不伪造空调用续轮 |
| 2026-09-17 | 第三轮独立审查修正 | GPT 基于 `a6c4eda` 复审：R2 十条中 7 条 ACCEPT，新出 4 条（T3-01～T3-04），主会话对照代码全部成立。改 Issue 正文：#118 AC-04 残留 grep 排除测试文件；#119 估算失败按「sampling Step 创建前 / plan 内」分两条验收，前者不调 LLM、后者投影 partial + estimator_failure 为预期，「真实 estimator 不同不阻塞」限定为历史选择差异；#116 adapter 第四条不变量加 `length` 例外并要求原始 chunk → adapter → Runtime 回归，上限下限改回 `>= 1 / >= 0` 保留 `1 / 0` 单轮禁工具模式，重复 id 判定限定在 `finalize()` 最终 id。#120 与两处流程修订 ACCEPT，未改 |
| 2026-09-17 | 第二轮独立审查修正 | GPT 基于 `2667027 → 67d4bad` 的独立审查给出 10 条 finding，主会话逐条对照本地代码核实全部成立。改 Issue 正文：#118 保留 tool Step.input 的 `executionAttempt` 持久化字段（Admin projector 必填）、smoke CLI 装配纳入范围、`tool-step-summary` 移出（非零行为变化）、加跨层验收；#119 `initialContext` 取裁剪前值以维持 Context Inspector 计数等式、`budget` 移到 `contextPlan`、AC-02 限定单调 estimator 并加真实 estimator 差分、加 Runtime→projector 回归；#120 同迁 `JsonSchemaProperty`、导入禁令只针对已迁移文件、七个 build 脚本与 workspace 依赖、干净产物验收；#116 两条补充评论并入正文，finalizer 只删两条 wire 重复、保留业务判定，Pi 消息类型为超集加一次转换。docs：pi-reference roadmap R2 允许 `agent` 依赖 `ai` 类型；mapping §5 改引用看板；workflow 加「补充决定并入正文」与「显式豁免预测题」两句 |
| 2026-09-17 | 去过度设计三件立项 | 2026-09-16 子代理对照 Pi 审查、主会话 grep 逐条核实后，用户破例不出预测题直接立 Issue：#118 删死代码与单实现抽象（零行为变化，约 800 行）、#119 历史裁剪合一（约 500 行）、#120 抽出 `packages/ai`（模型层脱离 Nest）。顺序 #118 → #119 → #120 → #115。核实中修正三处子代理结论：`retryable` 被 Admin 投影读取不删、`tool-evidence` 校验是 Grounding 语义不删、`snapshot().initialSelection` 被 sampling Step 读取只删 `items`。其余结构性问题归 #116 / R1 / R2，已写入 pi-reference roadmap |
| 2026-09-16 | 流程改为 AI 领节奏 | `workflow.md`：加触发语「下一步做什么」，AI 按路线提议、讲现有代码缺点、出预测题，多轮到用户能复述再建 Issue；实现默认另开会话只看 Issue；讨论不产出中间文件；路线守门；Issue 必须写「不过度设计」边界，review 把范围外抽象当 finding。删除「高风险 Issue 另开会话审 PR」（`/code-review` 的审查子代理本就不带实现上下文），改为 high 以上档位加故障注入场景必须进验收。skill 删预测题步、第 6 节复原 |
| 2026-09-16 | docs 与根目录清理 | 删除闲置文件：`docs/development-workflow.md`（双会话流程，9-05 起未用；双状态模型、docs 直写范围、Issue 模板搬入 `workflow.md` 第 3 节）、`development-task-plan.md`、`tasks/_template.tdd.md`、`PRODUCT.md`（6 月的 SEO 产品定义）、`PROJECT_STRUCTURE.md`（与 README / AGENTS.md 重复且过时）、`.codex/`、pi-reference 的 `source-files.txt`、三份 `*-coverage.md` 阅读证据表与 `checks/` 离线断言脚本。已完成横向任务 #92 / #94 / #98 / #101–#104 与 Admin Console 子任务文档移入 `tasks/completed/`。`docs/README.md`、`tasks/README.md` 重写为纯入口与看板；`roadmap.md` 删 Phase 8 两节重复归档、阶段 2–5 补归档链接；本文件删「已稳定的 Phase 8 事实」。docs 下 md 从 52 个减到 46 个，删除约 4000 行 |
| 2026-09-16 | 协作流程拆到 `docs/workflow.md` | `AGENTS.md` 第 5 节移出为 `docs/workflow.md`，`AGENTS.md` 用 `@docs/workflow.md` 导入（Claude Code 嵌套导入最多 4 层，路径相对含 `@` 的文件）；pi 适配加「先读该文件」。流程新增三个学习环节：开工前用户先预测当前代码行为、合并后带读收尾用户讲回来、用户在纯 TS 层独立改一处；「代码完成」与「学习已验证」分开记录。高风险 Issue（持久化 / 恢复 / fencing / 审批 / 鉴权 / migration）验收前另开无实现上下文的新会话审 PR，skill 在 PR 后停下等「继续」。用户改用只用 Claude Code，Codex 远程 Review 不再作为独立 review 来源 |
| 2026-09-16 | R0 之后加 `web_fetch` | 用户定案：#115–117 合并后、R2 之前立第一个真实工具 `web_fetch`（只读、SSRF 防护、untrusted observation），首个用途为盯 Pi 上游；`web_search` 进 R5 候选。工具在主线出现三次：#116 协议、R0 后 `web_fetch`、R3 工作区写入 |
| 2026-09-16 | 按独立审查稿修订 Pi 路线 | 用户采纳当日审查稿的部分结论：mapping §3 改为引用 R2 分包决定，删除「不先抽包」旧指令；R5 更正「MCP 是数据不是代码」；R2 写明最少持久集合与 accept 后崩溃重发现，R1 不再含 operation 身份；R3 验收补权限撤销、schema 变更、换 toolCallId 三条；R4 验收补慢订阅者有界；删除完成度百分比与工期合计；ACL / Auth 触发条件改为第一个外部写或第一次开放给第二个用户；总 roadmap 加学习出口。未采纳：企业交付阶段 F、本地链接改写、AGENTS.md 框架口径 |
| 2026-09-16 | R2 首步定为分包 | 用户定案：完成 #115–117 后，在 R2 开始时仿 Pi 分 `packages/agent`（零 Nest/Prisma）与 `packages/ai`，`apps/api` 只做宿主；新代码直接进包、旧代码按替换节奏迁入；Grounding 拆校验/落库；验收为包测试不启动 Nest、不连库。R5 候选新增 Skill（R2 后）与 MCP（R3 后，Pi 本版本无内置 MCP） |
| 2026-09-15 | Pi 路线改为实现口径 | 用户明确不读 Pi 代码，pi-reference 是给 AI 写代码用的素材：roadmap 去掉 L0–L5 学习站，改为 R0→R2→R1→R3→R4→R5 每步附“AI 查的素材”；how-to-read、learning-method 改为给实现智能体的说明；各入口与模块文档的“带读/学习”措辞同步 |
| 2026-09-15 | 收敛Pi学习路线 | 用户确认云端目标不需要终端、provider 生态：roadmap 学习站从 L0–L8 收成 L0–L5 六站并列出明确不学清单；重构顺序改为 R0→R2→R1→R3→R4→R5，补充完成度（约 25%～30%）与剩余工作量估算（15～18 周，不含 R5）；入口文档同步 |
| 2026-09-15 | Pi图表按反馈重排为自上而下 | 用户反馈图从左到右、流程不清、空白多字小、悬停动画异常：01/02/06/08 改为自上而下紧凑网格，03/07 改为时序图（用户 → 模型返回 toolCall 续轮 / 返回文本回复），全部开启 trace 动画；8 图 showcase 校验、build --check、build.test 通过。查看器字号由渲染器固定，只能靠压缩空白放大；浏览器视觉与悬停行为待用户允许后验证 |
| 2026-09-15 | Pi研究第二轮源码复核与完善 | 主会话通读 agent/ai/coding-agent/experimental/server/client/chord 核心源码，配合五路独立核查 149 条断言与 529 条链接：0 事实错误，约 20 处表述精确化与行号校正；新增 runtime 宿主 API 与错误契约、system prompt 装配、扩展事件目录、DeepSeek 专项、传输插槽与控制面认证、术语表 `glossary.md`、第 7 张图“普通 CLI 的一次 prompt”（含回复用户 / 工具续轮分支）、第 8 张图“生命周期与可定制点”、当前项目流协议/取消/零鉴权对照；verification 改为只记当前可复核状态。图表 build/--check/test 与两个离线断言脚本通过；第 7、8 张图未做人工视觉检查；未 commit |
| 2026-09-15 | Pi研究深度Review修复 | 校正SDK默认持久化与项目信任、共享Lane watch与客户端订阅，补充小默认能力/扩展工作流哲学及before_run_end重新检查inbox；图表构建检测并清理无源JSON的生成HTML，保护手写页面。六图同步检查与删除/重命名回归检查通过；用户已验收图集HTML |
| 2026-09-15 | Pi图表统一JSON维护与图集入口 | 源JSON集中到 `diagrams/specs/`；新增 `build.mjs` 从JSON生成六张图和index侧栏菜单，支持临时重建同步检查；`index.html` 以原生iframe切换图表，可直接本地打开。构建检查通过，Safari实际验证菜单逐项切换，未新增PNG或验收展示页 |
| 2026-09-15 | 精简Pi图表维护文件 | 按用户要求删除24张PNG、6个四图验收HTML、12份验收JSON；仅保留6组正式JSON/HTML及图表索引，补齐JSON输出路径并重新生成，6组全部9/9校验通过，生成HTML内容哈希与清理前一致；同步清理附件链接和维护说明 |
| 2026-09-15 | 修正Pi研究目录收口遗漏并删除旧资料 | 首轮遗漏顶层codex与旧learning-roadmap，用户指出后扩大检查范围；按用户明确要求删除旧Codex调研、reference、阶段路线及中间archive共65份Markdown，通用学习方法保留在pi-reference，清理相关入口与链接。首轮选定文件链接检查不能证明整个目录替换完整 |
| 2026-09-15 | Pi 全仓研究与参照入口替换 | 基于本机 Pi `8a7b0c03` 建立 `docs/research/pi-reference/`：覆盖11个主要包及根级工程资源，保存1714文件清单、核心源码注释、6张Archify图、AI带读说明、当前项目对照与学习/云端重构roadmap；原 `codex-reference` 11份文档移入 `research/archive/` 并标注历史快照；同步研究与阶段入口、AGENTS参照指向。研究不代表用户学完或重构启动，未修改产品代码与正式Task状态，未commit/push；实际检查边界见研究区 verification |
| 2026-09-12 | 单角色流程上收到 `AGENTS.md` | review 上一条重构发现单角色流程仍绑在只有 Claude 会读的 `CLAUDE.md`，换工具后无默认流程可循：单角色流程整体搬入 `AGENTS.md` 5.1 作为默认流程，硬约束归 5.2；`CLAUDE.md` 缩成 11 行适配层，只给 `<review 命令>` / skill 路径 / `<分支前缀>` 三个占位取值；`.codex/skills/github-issue-workflow` 的「另一侧」不再钉死为 GPT；`.claude/skills` 两个单角色 skill 改用 `<review 命令>` / `<分支前缀>` 占位成为共享实现；新增 pi 适配：`.pi/settings.json` 按 pi 官方写法引入 `../.claude/skills`，`.pi/APPEND_SYSTEM.md` 给三个占位取值（reviewer 子代理审暂存 diff、`pi/` 前缀）；查证 pi 0.85.1 每目录只取 `AGENTS.md`（有则忽略 `CLAUDE.md`），Claude Code 2.1.269 不原生发现 `.agents/skills`；`docs/README.md`、`docs/tasks/README.md`、`docs/roadmap.md`、`docs/development-workflow.md` 指向同步 |
| 2026-09-12 | 协作规范按载体分层重构 | `AGENTS.md` 收敛为工具无关基线（项目定位到 docs 规则 + 各流程共用硬约束），删除工具范围声明与双份同步条款；多角色分工流程归 `docs/development-workflow.md`，单角色流程归 `CLAUDE.md`（用 `@AGENTS.md` 引入基线），两份文件从 380 行含 310 行重复改为单一事实源；流程文档全文改用“你 / 另一侧”角色称呼，不再以工具名指代流程；review 改为 commit 前本地完成、PR 一律 Ready 创建（PR 仅作验收载体，Draft 只表示未完成或受阻，云端 Review 降为可选）；同步 `docs/README.md`、`docs/tasks/README.md`、`docs/roadmap.md`、`PROJECT_STRUCTURE.md`、`docs/research/codex-reference/discussion-playbook.md`（已删除）表述与两个 skills 的读取清单 |
| 2026-09-05 | 源码阅读立项健壮性三件 | 本机库 5 个 Run 全 COMPLETED、无失败样本，依据为源码阅读：`maxRetries: 0` 零重试且 Loop 默认上限 3 / 2（#115）；content 先于 tool_calls 或多个 tool_calls 直接 FAILED，而输入侧早已支持文本回填（#116）；DeepSeek Responses API 2026-08 上线且无状态，接入为第二个 adapter 而非替换（#117）。规格、验收标准、决策记录在 Issue，`docs/tasks` 只记状态；顺序 #115 → #116 → #117，一次一个 Active |
| 2026-09-05 | 删除翻译质检站代码与数据模型 | Issue #113 / PR #114，merge `26412d1`；Codex 远程 Review 无 findings，验收 AC-01～06 PASS；移除 `admin-qa` 模块、导入脚本、`contracts/admin-qa`、admin 五个 Qa 页面与 qa api / 路由 / 侧栏分组 / 中英 i18n 块、Prisma 7 模型 4 枚举与 `Article` 5 个质检字段（含无人读写的 `summary`）；新增 drop 迁移 `20260905120000_remove_qa_station` 并已应用于本机库，旧 4 条 qa 迁移保留；保留混入的通用 admin 改动（POST 支持、`refresh()`、配色）；本地 `data/snapshots/` 不动，`data/` 改由根 `.gitignore` 忽略 |
| 2026-09-05 | 方向定案与协作流程改版 | 两周方向调研（翻译质检站、内容流量助手、市场 C 端候选、公司后台候选、GitHub 开源借鉴）均未通过，最终定案：不再为 runtime 找产品域，作品是 runtime 本身，用户是自己，参照 Codex 与 DeepSeek Harness；`CLAUDE.md` 改为 Claude 单角色流程（聊清楚 → 建 Issue → 实现 → commit 前 `/code-review` → PR → 本会话逐条验收 → PASS 直接合并与收口，不依赖远程自动 Review），`AGENTS.md` 保留 GPT + Codex 双角色流程并与 CLAUDE.md 共用章节同步；两个 `.claude/skills` 同步改版；docs 状态对齐 |
| 2026-09-03 | 内容与流量调查助手放弃 | 两天做出 admin 流量总览与 GSC 接入后判断为仪表盘而非 agent，用户放弃；分支、代码、本地表与 `.env` 授权全部删除，master 无残留 |
| 2026-09-02 | 翻译质检站方向放弃 | 一天调查（源码、线上抽样、存量译文、GSC）证明现有翻译质量不差、剩余问题用不上 Agent；A-2 代码保留在 master，不再推进 A-3 及后续 |
| 2026-08-29 | 翻译质检站 A-2b 信息架构重构（待验收） | 用户验收反馈定案「档案与操作分离」，并授权免 Issue 在 `claude/issue-111-qa-station-a2` 分支继续开发：新增「质检工作台」菜单（文章队列、语种 chips、打分/审核/重译动作、Agent 翻译流程演示面板——五步时间线仅最后一步真实入队）；文章详情页改只读档案（语种矩阵含规则分/结论/审核状态、译文预览按需展开、沟通记录只读）；诊断对话落库（`QaDiagnoseMessage` 表 + 历史接口，应答仍为占位）；service 测试 15/15、真库自检通过、typecheck/lint/build 全绿，待用户验收 |
| 2026-08-29 | 翻译质检站 A-2 立项与实现（待验收） | Issue #111（Gate READY，用户授权 Claude 自行定夺方案）；`claude/issue-111-qa-station-a2` 实现：TranslationTask 表与 Score 增补字段、六个动作接口（详情/译文/打分-长度比最小真规则/审核附理由/任务幂等/诊断 mock）、admin 翻译质检页（质量结论/真实指标/判分过程优先，安全结构化全文对照按需展开，含动作区与诊断面板）；真库全链路自检通过、service 测试 13/13、typecheck、lint、admin checks 与 build 全绿；本地浏览器已核对分析层级、展开交互和无横向溢出，待用户验收 |
| 2026-08-29 | 翻译质检站 A-1 验收收口 | Issue #109 / PR #110；用户浏览器验收通过；PR 转 Ready 合并 `d2a2ba9`，Issue Closed，远程与本地任务分支已删除；docs 状态收口为 Completed；A-2（译文对照详情 + mock 动作）进入规格讨论 |
| 2026-08-29 | 翻译质检站 A-1 立项与实现（待验收） | 用户授权 SSH 生产服务器以 Laravel 只读导出快照至 `data/snapshots/2026-08-29/`（术语库 8/8725/156758 全量、文章元数据 1728、中文原文 3295、术语命中 top150 候选、候选全语言译文 2638，已 gitignore）；创建 Issue #109（Gate READY）并在 `claude/issue-109-qa-station-a1` 实现：温层 schema（ArticleTranslation、Glossary×3、TranslationScore 预建）、幂等导入脚本、admin-qa 只读三接口、admin「质检站」侧栏分组与文章/术语库/词条三页面；导入统计对账一致，服务层真库自检通过，三 workspace typecheck 与 lint 通过，service 测试 4/4；UI 页面效果待用户浏览器验收 |
| 2026-08-27 | Backend 模块组织 #104 验收收口 | Issue #104 / PR #108；Retrieval 按 Contract / Runtime、Retrievers、Persistence、Evaluation 分域，根目录只保留稳定入口；Retrieval 42、Tools 86、Article Indexing 62、Grounding 171 全绿，baseline 指标不变，API lint / typecheck / build 与 workspace typecheck 通过；DB integration 重构前后均因本机 5433 未启动受阻，未伪造通过；提交前 Review 修复 1 个 migration 路径 P2 后复审 `No findings.`，GitHub Codex Review 未发现 major issues；GPT 技术验收和用户确认通过；merge `132a51e`，Issue Closed，远程与本地任务分支已删除；Backend 模块组织 #101-#104 全部收口 |
| 2026-08-27 | Backend 模块组织 #103 验收收口 | Issue #103 / PR #107；保留 15 行 `article-chunking.ts` Facade，将 HTML 结构提取、确定性 Chunk 组装与 cl100k Token 计数拆入单向依赖的内部模块；Article Indexing 重构前后均 62、Retrieval 42 全绿，API lint / typecheck / build 与 workspace typecheck 通过；提交前 Review `No findings.`；GPT 技术验收和用户确认通过；merge `28024eb`，Issue Closed，远程与本地任务分支已删除；#104 推进为 Next / Gate PENDING |
| 2026-08-27 | Backend 模块组织 #102 验收收口 | Issue #102 / PR #106；Admin Runs 按 facade / context / retrieval / sampling usage / safe readers 分域并消除循环 import；Admin Runs 146、Grounding 171 与 Admin Overview / checks 全绿，API build / typecheck / lint、Admin 与 workspace typecheck 通过；提交前 Review `No findings.`，GitHub Codex Review 未发现 major issues；GPT 技术验收和用户确认通过；merge `638c70e`，Issue Closed，远程与本地任务分支已删除；#103 推进为 Next / Gate PENDING |
| 2026-08-27 | Backend 模块组织 #101 验收收口 | Issue #101 / PR #105；Agent Runtime 按 configuration / lifecycle / context / sampling / grounding 分域，根目录保留公共入口；Run Cancellation 提取为独立 Lifecycle 状态机并新增 7 条测试；提交前 xhigh Review `No findings.`，GitHub Codex Review 未发现 major issues；Lifecycle 21、Tool Loop 65、Model Stream 91、Context 24、Grounding 171、Admin Runs 146、SEO Service 32 全绿，build / typecheck / lint / diff check 通过；GPT 技术验收和用户确认通过；merge `2f1ae27`，Issue Closed，远程与本地任务分支已删除；#102 推进为 Next / Gate PENDING |
| 2026-08-26 | 失败 Sampling 部分响应可观测性 #98 验收收口 | Issue #98 / PR #100；失败、Abort、deadline 与消费者 return 路径保存 complete / partial / empty 聚合事实；Admin 区分未捕获、部分响应和空响应，复制 JSON 保留状态；安全日志只记录关联 ID 与计数；确定性测试 91 + 65 + 146 + 23 全绿，Admin checks、lint、typecheck、build 与 Chromium route fixture 通过；GPT 技术验收和用户确认通过；PR #100 merge `915315b`，Issue Closed，远程与本地任务分支已删除 |
| 2026-08-23 | DeepSeek 思考强度与 Usage #94 收口 | Issue #94 / PR #95；Web Low / High / Max 单次选择贯穿 resolved Run 配置与全部 sampling；Provider 显式 thinking / reasoning_effort 并移除 temperature；reasoning / cache Usage 持久化到 AgentStep，Admin Run Detail 按指标独立 all-or-nothing 展示；debug safe projection 剥离 reasoning_content；用户确认 UI 验收通过，独立技术复核后修正 reasoning E2E 精确定位，完整 Web Chromium 10 / 10；PR #95 merge `2266fad`，Issue #94 Closed / Completed |
| 2026-08-22 | Run 配置解析边界 #92 收口 | Issue #92 / PR #93；源码阅读阶段发现的横向 refactor：新增 `AgentRunConfigurationService` 收敛单次 Run 配置解析（policy getter 保 deadline 时序），`ResolvedChatRequestConfig` 补全 contextWindowTokens / temperature，Runtime 消除 `getModelProfile` 穿透与 `!` 断言，模型名单与 Profile 收敛进 `model-profiles.ts`，删除单行 `llm.constants.ts`；新增 `docs/research/configuration-map.md` 配置地图；commit 前 /code-review 15 项 findings（12 类修复并入提交，不修项在 PR 记录理由，含 model:'' 既有语义按规格保留）；验证 6 套件 383 项全绿 + lint + root typecheck；AC-01～09 逐项核对通过，用户确认验收并授权合并与清理；merge `f32cd48`，Issue #92 Closed，分支已删 |
| 2026-08-22 | Admin Enhancement 3 Overview 仪表盘 #90 收口 | Issue #90 / PR #91；stats + balance API（应用层扫 30 天窗口聚合 step JSON usage；DeepSeek 余额服务端代理复用 LLM_API_KEY）+ ECharts 按需注册前端（stat bar、双趋势图、比例条分布列表，亮暗主题联动）；/code-review 一轮 5 项全处理（含 resolvedModel 落库路径确认级 bug，实调接口验证）；视觉验收反馈一轮重构后用户确认并授权合并；实测确认 DeepSeek 官方 API 仅开放 user/balance，平台用量页无公开 API；merge `3108a5f`，Issue #90 Closed，分支已删 |
| 2026-08-22 | Admin Enhancement 2 会话记录入口 #88 收口 | Issue #88 / PR #89；会话列表 + 完整 transcript（会话级投影，run 级 500 字截断不变）+ runs conversationId 过滤 + 聊天气泡懒加载 UI + 路由滑动过渡；commit 前 /code-review 两轮 13 项（12 修 1 记录：runs 表格组件化留后续）；typecheck / lint / admin 检查 / api 144 测试全绿；用户确认验收并授权合并与分支清理；merge `e059cebb`，Issue #88 Closed，分支已删；同日创建 Enhancement 3 Overview 仪表盘 Issue #90（ECharts 选型与聚合方案已记录） |
| 2026-08-20 | Phase 8 任务目录归档 | `docs/tasks/phase-08-grounded-retrieval/` 全部文档核心内容合并归档到 `docs/tasks/completed/phase-08-grounded-retrieval.md`，原目录与阶段截图删除（浏览器证据以测试与 PR 记录为准）；docs 状态同步为 Phase 8 源码阅读阶段，下一阶段学习内容暂不定义 |
| 2026-08-19 | 开发环境自举缺陷 #84 收口 | 编写实机启动手册时实测发现：compose 主库镜像 `postgres:16-alpine` 不含 pgvector，`prisma:migrate` 必然在 Embedding Index migration 中断，本开发机主库长期仅应用 6/8 migration（缺 `ArticleChunk` / `ArticleIndexState` / `MessageGrounding`），web 前台 RAG 主链与 grounded 落库在 dev 主库上从未真正可用（Phase 8 验证均在 integration 库）；PR #85 将主库镜像对齐 `pgvector/pgvector:0.8.6-pg16-bookworm` 并补全 README quickstart（起库、seed、index 步骤与 collation 迁移注意）；本机主库重建实测：8/8 migration、seed 68 篇、index chunksWritten=2044 / failed=0、`smoke:retrieval-tool` 真实检索通过；用户确认合并，PR #85 merged，Issue #84 Closed |
| 2026-08-19 | 全仓源码审计后置维护 #72-#77 批量收口 | 基于 2026-08-18 三路并行源码审计（Runtime 核心 / RAG 链路 / 前端契约）创建 Issue #72-#77 并全部修复：#72 Runtime 终态收口（消费者 return() 兜底 + 在途请求取消 + terminalization_unknown 流通知，PR #78）、#74 finalization 模型不服从纳入 correction（新 rejection code `submission_missing`，PR #79）、#73 Web 流边界（reader.cancel、消息版本守卫、abort 占位幂等、onUnmounted，PR #80）、#75 quality-v2 lexical 基线与 hybrid 同源（PR #81）、#76 向量通道静默排除告警 + auxiliaryQuery 独立池（PR #82）、#77 卫生批量（PR #83）；每 PR 均经暂存区 code-review 自审（累计 11 个 finding 再修复）、Claude 逐项验收（PR 留档）、冷启动独立复审放行（3 项跟进已收口）；集成态验证 workspace typecheck 0 错、api 508 项 + web 44 项测试全绿；用户授权合并，PR #78-#83 全部 merge（`5071b71`～`28d3ea5`），Issue #72-#77 Closed，远程分支已清理；明确不修事项（C5 先说话再调工具、C6 tokenizer 性能、abortRun reason 元数据等）记录于各 PR |
| 2026-08-16 | Phase 8 后置维护 #66 最终收口 | Issue #66 / PR #71；最终验收 head `e22873c6aa0375750d4c2aeb0fb2d0e7a831272c`；GPT 基于最新 diff、精确 `@google/genai@2.17.1` SDK 源码、真实 SDK Transport 边界、Undici MockAgent 隔离、network / SDK timeout / caller Abort 三分支和重试计数核查确认 AC-01～AC-10 PASS；真实证据证明现有分类器正确，未修改 `gemini-embedding.provider.ts`；Codex 验证 `test:article-indexing` 62 / 62、专用 SDK boundary 测试 6 / 6、typecheck、lint 与 build 通过；用户明确确认验收并授权转 Ready、合并与文档收口；PR #71 merge `687e165966e3bfb3eed730f238d19082eddc5812`；Issue #66 Closed / Completed |
| 2026-08-16 | Phase 8 后置维护 #65 最终收口 | Issue #65 / PR #70；最终验收 head `ded42dcfe1d8914c694bba18455a8c33d4b54576`；GPT 基于最新 diff、DTO / ValidationPipe 信任边界、class-validator 实际语义、测试有效性和独立 ECMAScript 空白核查确认 AC-01～AC-06 PASS；Codex 验证 `test:seo-service` 31 / 31、编译产物 DTO 测试 9 / 9、typecheck、lint 与 build 通过；用户明确确认验收并授权转 Ready、合并与文档收口；PR #70 merge `9b417537d64be62aadc942c23e0f9574bd042687`；Issue #65 Closed / Completed |
| 2026-08-16 | Phase 8 后置维护 #64 最终收口 | Issue #64 / PR #69；最终验收 head `05ba8be85a3d2ba5999d84267135f7abe86521c7`；GPT 基于最新 diff、查询—投影链路、Service 接缝测试和独立行为核查确认 AC-01～AC-06 PASS；Codex 验证 `test:admin-runs` 139 / 139、typecheck 与 lint 通过；用户明确确认验收并授权转 Ready、合并与文档收口；PR #69 merge `e7cdb841839f19eaf4ce62be70faabbfced8edca`；Issue #64 Closed / Completed |
| 2026-08-16 | Phase 8 后置维护 #67 最终收口 | Issue #67 / PR #68；最终验收 head `e4a889970513b6563391545558136c6c21f834a1`；GPT 基于最新 diff、所有权语义、确定性竞态测试和独立 Node harness 确认 AC-01～AC-07 PASS；用户明确确认验收并授权转 Ready、合并与文档收口；PR #68 merge `51793f03a409a9f313584a6ddc2fba450fc33b70`；Issue #67 Closed / Completed |
| 2026-08-16 | Phase 8 Task 3C 最终收口 | Issue #62 / PR #63；最终验收 head `aadcadf510b20ea3c958b99ad1a8bfcf363dedf7`；GPT 多轮技术验收最终确认 AC-01～AC-12 PASS；用户明确确认验收并授权关闭 Issue、转 Ready、合并与 docs 收口；PR #63 merge `20f838fb1fd5139d787f973a90f4906d7ab8ea14`；Issue #62 Closed / Completed |
| 2026-08-16 | Phase 8 Task 3C 最终验证 | frozen install、contracts、API / Admin typecheck、lint、build、workspace typecheck 与 diff checks 通过；`test:admin-runs` 136、`test:grounding` 168、`test:grounding-db` 17，均 0 fail / 0 skip；Admin Chromium 12 / 12，repeat-each=3 为 36 / 36；根 lint 保留 113 个既有 Markdown baseline 错误 |
| 2026-08-16 | Phase 8 完成 | deterministic Chunking、Gemini Embedding / pgvector、lexical + vector RRF、Retrieval Tool、Grounding Session、structured finalization、durable Citation、Web Source UI 和 Admin Retrieval Inspector 全部闭环；阶段完成条件全部满足，Phase 8 状态更新为 Completed |
| 2026-08-16 | Phase 8 Task 3B 最终收口 | Issue #60 / PR #61；final head `516dbd3f`；AC-01～AC-12 PASS；PR #61 merge `572ad206271c0089eccc83e2a307bdb7909beeb1`；Issue #60 Closed |
| 2026-08-16 | Phase 8 Task 3A 最终收口 | Issue #58 / PR #59；final head `1e7f4c71`；AC-01～AC-24 PASS；PR #59 merge `d6df7ac1f24137a304748d21f4bca42dcb0a6ddc`；Issue #58 Closed |
| 2026-08-15 | Phase 8 Task 2B 最终收口 | Issue #56 / PR #57；AC-01～AC-16 PASS；merge `4f3ba1c109e8b0ade2328abeed24a72c295acd6d` |
| 2026-08-15 | Phase 8 Task 2A 最终收口 | Issue #54 / PR #55；68 / 68 Articles、2044 Chunks；quality-v2 完成；merge `3abdcb8afd5626f0b8fda90c98095bf529d165fd` |
| 2026-08-14 | Phase 8 Task 1 最终收口 | Issue #50 / PR #52；deterministic Chunking、Gemini profile、pgvector index、幂等 CLI；merge `76d66abf7af426e2a26f9b5765d1eb7a72382007` |
| 2026-08-14 | Phase 8 Task 0 最终收口 | Issue #48 / PR #49；Retrieval boundary、lexical baseline、Evaluation contract；merge `4c2f795084e7bccac205509d8c31b56dbe7ccf0b` |
| 2026-08-14 | Admin Enhancement 1 最终收口 | Issue #51 / PR #53；Run Trace Workspace；merge `159e964cafa081df218284b53f246a0da9edd04e` |
| 2026-08-13 | Phase 7 最终收口 | Issue #46 / PR #47；Context Engineering Completed；merge `caf3d25b`；Minimal Compaction Gated |

## 记录规则

- 只记录已经真实发生的事项。
- 研究定案、Issue 创建、实现、验收、Task 收口和合并是不同动作。
- Completed 必须有 PR 逐条验收 PASS 并合并的记录（`docs/workflow.md` 单角色流程）；早期按已删除的多角色流程（另一侧技术验收加用户确认）收口的记录保留原样。
- 不建 Issue 的小改动（`docs/workflow.md` 第 2 节的小改动例外）也记一行，写明 commit。
- 候选子系统未立 Issue 前不修改正式状态。
